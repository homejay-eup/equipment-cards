# 00_執行紀錄.md — 試做結果紀錄

> 每次子 Agent 執行完成後追加。只能追加，不修改既有條目。
> 格式：`## [YYYY-MM-DD] {專案ID} | Step {N} | {結果}`

---

## 使用說明

每筆記錄包含：
- **執行內容**：做了什麼
- **產出檔案**：新增或修改的檔案
- **遇到的問題**：執行中的阻礙
- **不滿意的點**：品質未達預期的地方
- **下一步建議**：後續行動

---

## [2026-05-23] P01 | 架構遷移 | 完成

**執行內容**：將舊版平鋪式 CLAUDE.md 結構遷移至 D 型架構（加入 _管理/、.claude/agents/、CoreBrain 連接）

**產出檔案**：
- `.claude/agents/frontend.md`（新增）
- `.claude/agents/data.md`（新增）
- `.claude/agents/tester.md`（新增）
- `.claude/agents/reviewer.md`（新增）
- `_管理/00_專案索引.md`（新增）
- `_管理/00_方案紀錄.md`（新增，含技術選型與 8 項需求決策）
- `_管理/00_執行紀錄.md`（新增，本檔案）
- `_管理/00_待整理清單.md`（新增）
- `_管理/01_equipment-cards/00_專案概覽.md`（新增）
- `_管理/01_equipment-cards/specs/`（新增 8 個規格文件）
- `CLAUDE.md`（改寫為 D 型格式）
- CoreBrain entity/analysis wiki（更新）

**遇到的問題**：無

**不滿意的點**：無

**下一步建議**：確認架構後，開始執行 Step 9（分類標籤更新）

---

## [2026-05-23] P01 | Step 12–15 | 完成

**執行內容**：
- Step 12 — PDF 文件外部連結：`documents JSONB` 欄位（SQL：需手動執行）、API 讀寫、CardDetailDialog 文件顯示、CardFormDialog 文件管理 UI
- Step 13 — 個人備忘板：`user_bookmarks` 資料表 + RLS（SQL：需手動執行）、/api/bookmarks CRUD、PhotoWall「我的關注」tab、CardDetailDialog ⭐ 按鈕 + 個人備註
- Step 14 — 鏡頭功能標籤：CardFormDialog tags datalist 建議、批次打標腳本（未執行）
- Step 15 — 搜尋擴充：Fuse.js keys 新增 category + documents.name

**產出檔案**：
- `src/types/equipment.ts`（新增 Document interface + documents 欄位）
- `src/app/api/cards/route.ts`（加入 documents）
- `src/app/api/cards/[id]/route.ts`（加入 documents）
- `src/app/api/bookmarks/route.ts`（新增）
- `src/app/api/bookmarks/[id]/route.ts`（新增）
- `src/components/CardDetailDialog.tsx`（文件區塊 + ⭐ + 個人備註）
- `src/components/CardFormDialog.tsx`（文件管理 UI + datalist）
- `src/components/PhotoWall.tsx`（我的關注 tab + Fuse keys）
- `_開發檔案/sql/add-documents.sql`（新增）
- `_開發檔案/sql/add-user-bookmarks.sql`（新增）
- `_開發檔案/scripts/tag-lens-cameras.js`（新增，未執行）

**遇到的問題**：
- Supabase DDL 無法自動執行（Management API 需 Personal Access Token），兩個 SQL 需手動到 Dashboard 執行
- Reviewer 發現 documents URL 需過濾 `javascript:` scheme（已修正）

**不滿意的點**：
- pending caption（待上傳細節照片的說明）是 Step 12 前就存在的舊 bug，此次未修正（範圍外）

**下一步建議**：
1. 手動執行兩段 SQL（見 `_開發檔案/sql/`）
2. 驗證 documents 和 bookmarks 功能是否正常
3. 執行 `tag-lens-cameras.js` 前先確認鏡頭料卡數量
4. Step 16（ESG 淨重標記）待排

---

## [2026-05-23] P01 | Step 9 修正 | 完成

**執行內容**：
- 移除 Step 14 加入的 datalist 下拉建議（tags 改回純文字輸入）
- 建立批次更新腳本 `update-categories-and-tags.js`：掃描 `_開發檔案/data/photos-categories` 資料夾，依檔名提取料號 → 建立標籤 map；依料號前綴自動指派分類（1→主機 / 2→鏡頭 / 3→螢幕 / 4→儲存設備 / 5-6→配件 / 7→線材或工具 / 9→耗材或工具，其他→其他）；同步更新 `app_settings` 分類清單
- 執行腳本：487 筆料卡分類更新、488 個料號取得標籤對應（509 筆）、0 失敗
- Build 通過，推送 main 觸發 Vercel 自動部署

**產出檔案**：
- `_開發檔案/scripts/update-categories-and-tags.js`（新增）
- `src/components/CardFormDialog.tsx`（移除 datalist）

**遇到的問題**：
- 腳本路徑錯誤：worktree 中 `__dirname` 深 5 層，需 `path.join(__dirname, '../../../../../')` 才能到主專案根目錄

**不滿意的點**：無

**下一步建議**：
1. 驗證線上分類、標籤是否顯示正確
2. Step 16（ESG 淨重標記）低優先，依需要再排
