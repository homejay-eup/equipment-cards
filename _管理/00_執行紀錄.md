# 00_執行紀錄.md — 試做結果紀錄

> 每次子 Agent 執行完成後追加。只能追加，不修改既有條目。
> 格式：`## [YYYY-MM-DD] {專案ID} | Step {N} | {結果}`

---

## 使用說明

每筆記錄包含：
- **執行內容**：做了什麼
- **產出檔案**：新增或修改的檔案
- **遇到的問題**：執行中的阻礙
- **不滿意的點**：品質未達預期的地方
- **下一步建議**：後續行動

---

## [2026-06-11] P01 | Step 27–31 | 完成

- **執行內容**：部門重構 + 邏輯審查修正集（共 5 個 Sub-step）
  - **Step 27**：`assignable_role_names` cascade 修復 — 角色改名/刪除時同步更新所有引用該名稱的角色陣列（PATCH/DELETE `/api/roles/[id]`）
  - **Step 28**：系統角色權限保護 — `super_admin` 的 `manage_roles`/`manage_users` 前後端雙層鎖定；「重設為預設」按鈕移入「可指派角色」區塊；部門下拉改為從後端動態查詢（`Department[]` 型別）
  - **Step 29**：`dept_group TEXT` → `departments` 資料表重構 — 新增 `departments` 表 + RLS；`roles` / `issues` 加 `department_id UUID FK`；舊欄位刪除；SQL migration 已執行
  - **Step 30**：DepartmentsManager UX 重寫 — CRUD（新增/改名/刪除部門）、角色-部門指派、未分配角色 banner、ConfirmDialog 破壞性保護
  - **Step 31**：全專案健康檢查 — 補齊 `VALID_PERMISSION_KEYS` 缺漏的 3 個 key（read_tags/read_weight/read_created_at）；清除所有 `dept_group` 舊命名殘留；cascade loop 靜默錯誤修復（收集失敗 ID 回 warning）
- **新增檔案**：
  - `src/app/api/departments/route.ts`
  - `src/app/api/departments/[id]/route.ts`
  - `_開發檔案/sql/step26-departments-refactor.sql`
- **修改的既有檔案**：
  - `src/components/DepartmentsManager.tsx`（完整重寫）
  - `src/components/RolesManager.tsx`（dept_group → Department[]；is_system 保護；按鈕移位）
  - `src/app/admin/departments/page.tsx`
  - `src/app/admin/roles/page.tsx`
  - `src/app/admin/users/page.tsx`
  - `src/app/api/roles/[id]/route.ts`（cascade + 錯誤收集）
  - `src/app/api/roles/[id]/permissions/route.ts`（補 3 個 key）
  - `src/app/api/roles/route.ts`（department_id join）
  - `src/app/api/roles/assignable/route.ts`（註解更新）
  - `src/app/api/admin/users/route.ts`（dept → department_id；sameGroup → sameDept）
  - `src/app/api/issues/route.ts`（dept_group → department_id）
  - `src/app/tracker/page.tsx`（dept_group → department_id）
  - `src/lib/admin.ts`（AssignableRoleRow 更新；JSDoc 更新）
- **build 結果**：✓ 通過，無新增錯誤
- **已清除技術債**：00_待整理清單.md 中所有 [Bug]/[技術債]/[UX] 項目均已解決

---

## [2026-06-10] P01 | Step 26 | 完成

- **執行內容**：邏輯審查修正集（9 項，含 reviewer 追加 4 項）
- **修改的既有檔案**：
  - `src/lib/admin.ts`（移除廢棄 key；新增 getAssignableRolesData、export isAllowedDomain）
  - `src/app/api/admin/users/route.ts`（domain 修正；PATCH/DELETE email normalize）
  - `src/app/api/issues/route.ts`（GET 加 dept_group 過濾；catch 加 error log）
  - `src/app/tracker/page.tsx`（super_admin 不加 dept_group 過濾，可看全部議題）
  - `src/app/api/roles/[id]/route.ts`（rename cascade 更新 allowed_emails；失敗回 207）
  - `src/app/api/roles/assignable/route.ts`（改呼叫共用函數，消除重複邏輯）
  - `src/app/api/roles/route.ts`（GET/POST 補 dept_group + level 欄位）
  - `src/app/admin/users/page.tsx`（dept_admin 只看本部門帳號；null safety 修正）
  - `src/components/RolesManager.tsx`（新增角色表單加 dept_group/level；移除廢棄 UI 選項）
- **Reviewer 追加修正**：
  - S2：cascade 失敗原回 200 → 改 207，前端可感知警告
  - S1：catch block 加 console.error，DB 故障時可排查
  - M1：user null 未保護 → 加 `if (!admin || !user) redirect`
  - M3：DELETE 缺 email normalize → 補上
  - M4：RolesManager UI 仍顯示廢棄 filter_all_statuses / filter_no_photo → 移除
- **build 結果**：✓ 通過，無新增錯誤
- **已更新常駐檔**：00_執行紀錄.md、00_專案概覽.md、00_專案索引.md、step26-logic-audit-fixes.md（進度追蹤）

---

## [2026-06-09] P01 | Step 25 | 完成

- **執行內容**：Bug 修正集（7 項 + 1 項新需求）
- **產出檔案**：
  - 新建：`_開發檔案/sql/step25-bug-fixes.sql`、`_管理/01_equipment-cards/specs/step25-bug-fixes.md`
  - 修改：`src/app/api/cards/[id]/route.ts`、`src/app/api/settings/route.ts`、`src/components/UserManagementTable.tsx`、`src/app/admin/users/page.tsx`、`src/components/CardDetailDialog.tsx`、`src/components/PhotoWall.tsx`、`src/components/RolesManager.tsx`、`src/components/IssueDetailDialog.tsx`
- **修正項目**：
  1. 追蹤板隔離：SQL backfill issues.dept_group
  2. 角色管理按鈕加 manage_roles 條件
  3. 有 edit_card_* 即顯示料卡編輯按鈕（不再依賴 isAdmin）
  4. PATCH /api/cards/[id] permission 從廢棄的 crud_cards 改為 create_delete_cards / edit_card_*
  5. PATCH /api/settings permission 放寬給 edit_card_category / edit_card_status
  6. CardDetailDialog caption 底色 .65 → .38
  7. 新增 read_tags / read_weight / read_created_at 三個 permission key
  8. IssueDetailDialog 狀態標籤改為純靜態 badge
- **Reviewer 發現並修正**：
  - /api/settings 缺欄位層級隔離（edit_card_category 可改 issueTypes），補上 allowedKeys 過濾
  - /api/cards/[id] 缺欄位層級過濾（edit_card_name 可改 status），補上 allowedUpdates 過濾
  - /api/settings 缺 session check，補上 getUser 驗證
  - PhotoWall canEditCard 包含 isAdmin 與 API 邏輯不一致，移除 isAdmin
- **需手動執行 SQL**：`_開發檔案/sql/step25-bug-fixes.sql`（issues.dept_group backfill + 新增三個 read_ permissions）
- **已更新常駐檔**：00_執行紀錄.md、00_專案概覽.md

---

## [2026-06-08] P01 | Step 23 | 完成（含 SQL 執行）

## [2026-06-09] P01 | Step 24 | 完成

- **執行內容**：任務板改版與修正（10 項）
- **產出檔案**：
  - 新建：`src/app/admin/departments/page.tsx`、`src/components/DepartmentsManager.tsx`、`_開發檔案/sql/step24-dept-backfill.sql`
  - 修改：`api/issues/route.ts`、`api/roles/[id]/route.ts`、`auth/callback/route.ts`、`tracker/TrackerClient.tsx`、`tracker/page.tsx`、`IssueDetailDialog.tsx`、`PhotoWall.tsx`、`RolesManager.tsx`、`UserMenu.tsx`、`lib/admin.ts`
- **遇到的問題**：Step 23 SQL 的 issues 回填邏輯用 `profiles` 表（本專案不存在），補寫正確版 SQL 用 `allowed_emails`
- **Reviewer 發現並修正**：domain 驗證改嚴格比對（`split('@')[1]` + `includes`）、null dept_group 使用者回傳空 issues
- **不滿意的點**：IssueDetailDialog 狀態選單用 onMouseLeave 關閉，行動裝置體驗次佳（技術債）
- **建議下一步**：PR #16 合併後觀察實際使用，確認部門隔離行為符合預期
- **已更新常駐檔**：00_執行紀錄.md、00_專案索引.md、00_專案概覽.md

**執行內容**：角色群組擴充，前後端程式碼 + SQL 全部完成，build 通過，Vercel 已部署

**產出檔案**：
- `_開發檔案/sql/step23-role-group-expansion.sql`（新建，已在 Supabase 執行完畢）
- `src/app/api/roles/assignable/route.ts`（新建）
- `src/app/api/roles/[id]/permissions/route.ts`（修改：白名單更新）
- `src/app/api/admin/users/route.ts`（修改：指派限制 + null guard 安全修正）
- `src/components/RolesManager.tsx`（修改：UI 按頁面分組、父子連動、移除群組功能、加可編輯議題）
- `src/components/CardFormDialog.tsx`（修改：edit_field_* → edit_card_*）
- `src/app/admin/roles/page.tsx`（修改：加 dept_group/level）
- `src/app/admin/users/page.tsx`（修改：改呼叫 assignable API）

**遇到的問題**：
- SQL 中 `tracker_issues` 表名錯誤（實際為 `issues`），導致 ERROR 42P01，已修正
- `issues.created_by` 是 TEXT，`profiles.id` 是 UUID，需加 `::text` 型別轉換
- SQL tracker key 名稱與現有程式碼不一致（tracker_my_tasks vs view_my_tasks），已修正
- getCallerRoleInfo() null 時安全漏洞，reviewer 發現後修正
- edit_field_* → edit_card_* 更名首次未執行完整，tester 發現後補修
- 前端程式碼跨 main repo 和 worktree 分散，最終同步至 main repo 後一次 commit

**下一步**：Step 23 完成，可依需求討論 Step 24 或其他後續功能

---

## [2026-06-07] P01 | Step 23 | 規格完成（需求討論）

**執行內容**：需求討論並收斂角色群組擴充設計，產出 Step 23 規格文件

**討論決策**：
- 角色內建群組（12 個系統角色），指派角色即決定群組，不另外設定
- 三種資料範圍固定：私有（個人）/ 群組（部門公佈欄）/ 全域
- 追蹤板永遠只看自己 dept_group，包含管理員
- 權限 UI 按頁面分組（料卡列表 / 料卡細節 / 料卡管理 / 帳號管理 / 追蹤板）
- 新增 12 個 `edit_card_*` 細粒度欄位編輯 permission key
- 移除「群組功能」勾選項，追蹤板加「可編輯議題」
- `crud_cards` 拆分為 `create_delete_cards` + `edit_card_*`
- dept_admin 指派限制：只能指派同 dept_group 且 level = member/viewer

**產出檔案**：
- `_管理/01_equipment-cards/specs/step23-role-group-expansion.md`（新增）

**下一步建議**：開新 Session 執行 Step 23，依規格委派 data + frontend

---

## [2026-05-23] P01 | 架構遷移 | 完成

**執行內容**：將舊版平鋪式 CLAUDE.md 結構遷移至 D 型架構（加入 _管理/、.claude/agents/、CoreBrain 連接）

**產出檔案**：
- `.claude/agents/frontend.md`（新增）
- `.claude/agents/data.md`（新增）
- `.claude/agents/tester.md`（新增）
- `.claude/agents/reviewer.md`（新增）
- `_管理/00_專案索引.md`（新增）
- `_管理/00_方案紀錄.md`（新增，含技術選型與 8 項需求決策）
- `_管理/00_執行紀錄.md`（新增，本檔案）
- `_管理/00_待整理清單.md`（新增）
- `_管理/01_equipment-cards/00_專案概覽.md`（新增）
- `_管理/01_equipment-cards/specs/`（新增 8 個規格文件）
- `CLAUDE.md`（改寫為 D 型格式）
- CoreBrain entity/analysis wiki（更新）

**遇到的問題**：無

**不滿意的點**：無

**下一步建議**：確認架構後，開始執行 Step 9（分類標籤更新）

---

## [2026-05-23] P01 | Step 12–15 | 完成

**執行內容**：
- Step 12 — PDF 文件外部連結：`documents JSONB` 欄位（SQL：需手動執行）、API 讀寫、CardDetailDialog 文件顯示、CardFormDialog 文件管理 UI
- Step 13 — 個人備忘板：`user_bookmarks` 資料表 + RLS（SQL：需手動執行）、/api/bookmarks CRUD、PhotoWall「我的關注」tab、CardDetailDialog ⭐ 按鈕 + 個人備註
- Step 14 — 鏡頭功能標籤：CardFormDialog tags datalist 建議、批次打標腳本（未執行）
- Step 15 — 搜尋擴充：Fuse.js keys 新增 category + documents.name

**產出檔案**：
- `src/types/equipment.ts`（新增 Document interface + documents 欄位）
- `src/app/api/cards/route.ts`（加入 documents）
- `src/app/api/cards/[id]/route.ts`（加入 documents）
- `src/app/api/bookmarks/route.ts`（新增）
- `src/app/api/bookmarks/[id]/route.ts`（新增）
- `src/components/CardDetailDialog.tsx`（文件區塊 + ⭐ + 個人備註）
- `src/components/CardFormDialog.tsx`（文件管理 UI + datalist）
- `src/components/PhotoWall.tsx`（我的關注 tab + Fuse keys）
- `_開發檔案/sql/add-documents.sql`（新增）
- `_開發檔案/sql/add-user-bookmarks.sql`（新增）
- `_開發檔案/scripts/tag-lens-cameras.js`（新增，未執行）

**遇到的問題**：
- Supabase DDL 無法自動執行（Management API 需 Personal Access Token），兩個 SQL 需手動到 Dashboard 執行
- Reviewer 發現 documents URL 需過濾 `javascript:` scheme（已修正）

**不滿意的點**：
- pending caption（待上傳細節照片的說明）是 Step 12 前就存在的舊 bug，此次未修正（範圍外）

**下一步建議**：
1. 手動執行兩段 SQL（見 `_開發檔案/sql/`）
2. 驗證 documents 和 bookmarks 功能是否正常
3. 執行 `tag-lens-cameras.js` 前先確認鏡頭料卡數量
4. Step 16（ESG 淨重標記）待排

---

## [2026-05-23] P01 | Step 9 修正 | 完成

**執行內容**：
- 移除 Step 14 加入的 datalist 下拉建議（tags 改回純文字輸入）
- 建立批次更新腳本 `update-categories-and-tags.js`：掃描 `_開發檔案/data/photos-categories` 資料夾，依檔名提取料號 → 建立標籤 map；依料號前綴自動指派分類（1→主機 / 2→鏡頭 / 3→螢幕 / 4→儲存設備 / 5-6→配件 / 7→線材或工具 / 9→耗材或工具，其他→其他）；同步更新 `app_settings` 分類清單
- 執行腳本：487 筆料卡分類更新、488 個料號取得標籤對應（509 筆）、0 失敗
- Build 通過，推送 main 觸發 Vercel 自動部署

**產出檔案**：
- `_開發檔案/scripts/update-categories-and-tags.js`（新增）
- `src/components/CardFormDialog.tsx`（移除 datalist）

**遇到的問題**：
- 腳本路徑錯誤：worktree 中 `__dirname` 深 5 層，需 `path.join(__dirname, '../../../../../')` 才能到主專案根目錄

**不滿意的點**：無

**下一步建議**：
1. 驗證線上分類、標籤是否顯示正確
2. Step 16（ESG 淨重標記）低優先，依需要再排

---

## [2026-05-23] P01 | Step 16 Phase 1 | 完成（SQL 待手動執行）

**執行內容**：ESG 淨重照片標記 — Schema + API + 前端全面實作

**產出檔案**：
- `_開發檔案/sql/add-net-weight.sql`（新增）
- `_管理/01_equipment-cards/specs/esg-net-weight.md`（新增）
- `src/types/equipment.ts`（修改：加入 `net_weight`, `weight_photo`, `weight_photo_public_id`）
- `src/app/api/cards/route.ts`（修改：POST 加入 `net_weight`）
- `src/app/api/cards/[id]/route.ts`（修改：PATCH 加入 `net_weight` + 淨重照刪除邏輯；DELETE 加入 Cloudinary 清除）
- `src/app/api/upload/route.ts`（修改：PATCH 新增 `type: 'weight'` 分支）
- `src/app/api/cards/batch/route.ts`（修改：upsert 行為，回傳含 `updated` 計數）
- `src/components/CardFormDialog.tsx`（修改：淨重輸入 + 淨重照上傳暫存機制）
- `src/components/CardDetailDialog.tsx`（修改：淨重值顯示 + 淨重照加入輪播）
- `src/components/BatchImportDialog.tsx`（修改：header-based CSV 解析 + 淨重欄支援 + BOM 修正）

**遇到的問題**：
- `net_weight = 0` 的字串 falsy 問題（`'0' ? ...` 走 null），改為 `!== ''` 判斷修正
- PATCH /api/cards/[id] 已在 server 端 destroy Cloudinary，client 端不需重複呼叫 DELETE /api/upload
- Excel UTF-8 CSV 帶 BOM 字元導致 header 比對失敗，加 `text.replace(/^﻿/, '')` 修正

**不滿意的點**：無

**下一步建議**：
1. 手動在 Supabase Dashboard 執行 `_開發檔案/sql/add-net-weight.sql`
2. Step 16 Phase 2：等淨重照片提供後，執行批次照片上傳腳本

---

## [2026-06-07] P01 | Step 21 | 完成

**執行內容**：追蹤板 14 項改善（改善 13、14 不動）

**產出檔案**：
- `src/app/tracker/page.tsx`（修改：改為 SSR 渲染 TrackerClient，移除 TrackerList 依賴）
- `src/app/tracker/TrackerClient.tsx`（修改：移除 ArrowLeft 返回按鈕、兩行式佈局、formatDatetime）
- `src/components/IssueDetailDialog.tsx`（修改：Optimistic Update x2、建立者移入 meta grid、formatDatetime、SettingsPopover 齒輪）
- `src/components/NewIssueDialog.tsx`（修改：SettingsPopover 齒輪）
- `src/components/SettingsPopover.tsx`（修改：加入 issueTypes settingKey）
- `src/app/api/settings/route.ts`（修改：PATCH 白名單加入 issueTypes、issueTags）

**遇到的問題**：
- Cherry-pick 時 remote 已重構 tracker 架構（TrackerList → TrackerClient），需整合衝突
- Reviewer 發現 /api/settings PATCH 白名單缺少 issueTypes（Critical）→ 已修正

**不滿意的點**：無

**下一步建議**：git push → Vercel 部署

---

## [2026-06-14] P01 | Step 29 | ✅ 成功

**完成項目**：資料管線腳本（批次匯入 + 全量備份）

**產出檔案**：
- `_開發檔案/scripts/batch-import.ts`（新建）
- `_開發檔案/scripts/export-backup.ts`（新建）
- `_開發檔案/data/photos-weight/`（新建資料夾）
- `_管理/01_equipment-cards/specs/step29-data-pipeline.md`（新建規格文件）

**執行驗證**：
- `npx tsx batch-import.ts --dry-run`：data 目錄無資料時正常印出「無資料」並結束
- `npx tsx export-backup.ts`：成功連線 Supabase、讀取 788 筆、輸出 JSON 到 `_開發檔案/backup/2026-06-14/`

**設計重點**：
- batch-import 的 Replace 流程：查 DB → Cloudinary destroy 刪舊照片 → DELETE DB → 重新上傳 + INSERT
- export-backup 使用分頁查詢（每次 1000 筆），避免 Supabase 單次回傳限制
- 照片資料夾以名稱判斷類型（photos-main / photos-detail / photos-weight）
- Cloudinary 上傳使用 ReadStream + upload_stream（修正 Windows 中文路徑問題）
- env 路徑指向 `../../.env.local`

**不滿意的點**：無

**下一步建議**：準備好 manifest.csv 與照片後，執行 `--dry-run` 確認解析結果再正式匯入

---

## [2026-06-21] P01 | Bug 修正：任務類型按部門隔離 | 完成

- **執行內容**：
  1. **Bug 1 — 非管理員無法儲存類型選項**：`PATCH /api/settings` 的權限白名單加入 `create_issues`、`tracker_edit_issue`；`EditIssueDialog` 增加 `canManageIssueTypes` prop，傳入 `IssueDetailDialog` / `IssueExpandedContent`，無權限時 SettingsPopover 顯示 `disabled`
  2. **Bug 2 — issueTypes/issueTags 全部門共用**：新建 `department_issue_types` 資料表（`department_id` PK，`types` JSONB，`tags` JSONB）；API 寫入改走此表，依呼叫者 email → role → department_id 隔離；`page.tsx` / `tracker/page.tsx` 讀取改查此表，預設空陣列；RLS 已啟用（service role 繞過，讀寫不受影響）

- **新增/修改的檔案**：
  - `src/app/api/settings/route.ts`（權限白名單 + 部門隔離寫入）
  - `src/components/EditIssueDialog.tsx`（canManageIssueTypes prop + disabled）
  - `src/components/IssueDetailDialog.tsx`（傳入 canManageIssueTypes）
  - `src/components/IssueExpandedContent.tsx`（傳入 canManageIssueTypes）
  - `src/app/page.tsx`（getTrackerData 改查 department_issue_types）
  - `src/app/tracker/page.tsx`（第三批改查 department_issue_types）
  - `_開發檔案/sql/add-department-issue-types.sql`（新增）

- **DB Migration**：在 Supabase SQL Editor 執行 `CREATE TABLE IF NOT EXISTS department_issue_types`，RLS 已啟用。各部門從空陣列開始自行建立。

- **遇到的問題**：
  - push 前遠端已有新 commit（deferred mode 重構），需 stash + pull rebase + stash pop 處理合併衝突
  - EditIssueDialog.tsx 合併後留有 `<<<<<<< Updated upstream` 殘留標記，需額外修補
  - tracker/page.tsx 移除 getSettings() 後 unused variable lint 錯誤

- **build 結果**：✓ 通過
- **不滿意的點**：無
- **下一步建議**：各部門登入後可自行設定任務類型與標籤

## [2026-06-21] equipment-cards | Step 23 補丁 | ✓ 成功

- **任務**：Realtime 即時同步上線後的問題修正

- **問題 1：RLS policy 使用錯誤的 auth 來源**
  - 現象：Realtime 訂閱無任何事件推送
  - 根因：issues 表 RLS "group scoped tracker" 使用 profiles + auth.uid()，但 profiles.role 欄位未填；Realtime 走 anon key + JWT，受 RLS 控制，查詢始終返回 false
  - 修法：DROP 舊 policy，改用 allowed_emails + auth.email()（與專案實際 auth 架構一致）
  - SQL 執行於 Supabase SQL Editor → Success

- **問題 2：normalizeIssue 移除 issue_assignees 導致負責人欄位清空（CodeGraph 漏查）**
  - 根因：實作時未做 CodeGraph blast radius check，未 Grep GET/PATCH /api/issues/[id] 的所有呼叫端
  - 受影響元件：IssueDetailDialog（refresh 時）、IssueExpandedContent（換狀態後）、EditIssueDialog（儲存後）—— 三個元件都自行從 issue_assignees 正規化
  - 修法：normalizeIssue 移除 `issue_assignees: undefined`，只新增 assignee_emails/assignees，不刪舊欄位
  - commit: 55c2536

- **教訓**：API route 回傳格式變更前，必須 Grep 所有呼叫端確認消費方式，不可只看 Realtime hook 單一消費者

- **build 結果**：✓ 通過（0 errors）
- **部署**：push → Vercel 自動部署

## [2026-06-21] equipment-cards | Step 30 Phase 1 | ✓ 成功

- **任務**：A++ 產品規格書批次上傳 Google Drive + 料卡 documents 欄位回填

- **產出腳本**：
  - `_開發檔案/scripts/upload-spec-books.py` — Google Drive OAuth 上傳腳本
  - `_開發檔案/scripts/update-card-documents.py` — Supabase documents 欄位寫入腳本

- **執行結果**：
  - Google Drive 根資料夾：`A++ 產品規格書`（ID: `1Ggh_9FvKhLNcBtv2HR4lW-cSWN2CyMJt`）
  - 上傳結構：5 類別 → 92 條目 → 184 個檔案（PDF / JPG）
  - Supabase documents 更新：91 筆（成功） / 0 失敗
  - 2000061 (HS-CC300) 有兩個資料夾（中文/英文版），documents 合併為 1 筆

- **注意**：
  - FH 系列 7 個資料夾嵌套於 `車機/馥鴻DVR/` 下，腳本已加 fallback 搜尋一層子目錄
  - Drive OAuth token 已存至 `_開發檔案/drive-token.json`（勿 commit）
  - `_開發檔案/logs/spec-book-links.json` 保存完整的 equipment_id → documents 對照

- **待執行（Phase 2）**：前端 CardDetailDialog 顯示 documents 連結 UI（若尚未實作）

---

## [2026-07-08] equipment-cards | Step 31 | ✓ 成功

- **背景**：帳號管理頁面要一個個手動輸入 Email 太耗時，且外包技師的個人 Gmail 需要能登入看料卡。

- **關鍵發現（執行前 CodeGraph + Grep 調查)**：
  - 登入闸门其實只有 `auth/callback/route.ts` 一處硬寫網域陣列，`allowed_emails` 表本身早就允許任意 email 格式寫入，但沒有反過來影響登入闸门 —— 導致清單裡已存在的 `jenny@gmail.com`（供應鏈角色）實際上登入不了。
  - `auth/callback/route.ts` 的 `ALLOWED_DOMAINS` 沿用 `eup.com.vn`（錯字），與 `admin.ts` 先前已修正的 `eup.net.vn` 不同步 —— 這是 2026-06-13 testing playbook 記錄過的 PRE-01，只修了 `admin.ts` 沒修到 `callback/route.ts`，本次一併補上。

- **執行內容**：
  1. `src/lib/admin.ts` 新增 `isEmailAllowedToLogin(email)`：網域符合 `isAllowedDomain` 或 email 已存在於 `allowed_emails` 表即放行。
  2. `src/app/auth/callback/route.ts`：移除寫死且有 bug 的 `ALLOWED_DOMAINS`，改呼叫 `isEmailAllowedToLogin`。
  3. 新增 `src/app/api/admin/sync-users/route.ts`（POST，限 `super_admin`）：分頁抓取 Supabase Auth 已登入使用者、篩公司網域、比對現有 `allowed_emails`、把缺少的帳號 `upsert`（`onConflict: email, ignoreDuplicates: true`）寫入並預設角色「一般使用者」，回傳新增清單。
  4. `src/components/UserManagementTable.tsx` 新增「同步公司帳號」按鈕（僅 `canSyncUsers` 為 true 時顯示），呼叫上述 API 並把結果併入清單，含 loading 狀態與成功/失敗訊息。
  5. `src/app/admin/users/page.tsx` 傳入 `canSyncUsers={callerLevel === 'super_admin'}`（沿用既有變數，未新增查詢）。

- **修改的既有檔案**：`src/lib/admin.ts`、`src/app/auth/callback/route.ts`、`src/components/UserManagementTable.tsx`、`src/app/admin/users/page.tsx`
- **新增檔案**：`src/app/api/admin/sync-users/route.ts`

- **Tester 驗證**：登入闸门正常路徑/邊界情境、同步 API 權限/分頁/去重/空結果、前端渲染條件與去重邏輯皆確認無誤。點出既有技術債（`allowed_emails` 查詢多處未做 email 大小寫 normalize、DELETE handler 同樣缺 normalize）非本次引入，未修正。

- **Reviewer 發現並修正**：
  - sync-users 的 insert 改成 `upsert` + `ignoreDuplicates`，避免兩人同時點同步或跟手動新增撞期時，因唯一鍵衝突導致整批新帳號寫入失敗。
  - 其餘中低項目（`getCallerRoleInfo` 在兩個 route 檔重複、缺乏稽核欄位如 `added_by`）記為技術債，未處理。

- **未解決／待人工確認（重要）**：
  - Reviewer 標記高風險項目：`_開發檔案/sql/` 裡找不到 `allowed_emails`／`roles`／`role_permissions` 三張表的 RLS policy 腳本，無法從程式碼確認這三張表是否已鎖死只給 `service_role` 存取。**這不是本次改動造成的，但無法在程式碼層級修復，需要使用者直接到 Supabase Dashboard 確認這三張表的 RLS 設定**，若未鎖死，任何登入過的人理論上可繞過應用層檢查直接竄改這幾張表。

- **build 結果**：✓ 通過（0 errors，僅 1 個與本次無關的既有 ESLint warning）
- **不滿意的點**：RLS 狀態無法從 repo 驗證，此項待使用者手動確認 Supabase Dashboard。
- **建議下一步**：
  1. 到 Supabase Dashboard 確認 `allowed_emails`/`roles`/`role_permissions` 的 RLS policy。
  2. 實際測試：用一個未被加入清單的外部 Gmail 帳號登入，確認會被擋；把它加進帳號管理清單後再登入一次，確認能通過。
  3. 以 super_admin 身分點擊「同步公司帳號」，確認公司網域帳號正確被抓進清單。
- **已更新常駐檔**：00_執行紀錄.md、01_equipment-cards/00_專案概覽.md

---

## [2026-07-08] equipment-cards | Step 31 後續 | RLS 確認與補強 | ✓ 完成（已修復高風險漏洞）

- **背景**：延續上方 Step 31 條目「未解決／待人工確認」項目，確認 `allowed_emails`／`roles`／`role_permissions` 三張表的 RLS 狀態。

- **執行方式**：因無 Supabase CLI / psql / DB 連線字串可用，改用 REST API（`curl` + `.env.local` 的 anon key／service_role key）直接對三張表做匿名讀寫實測，比對 Dashboard 畫面更可靠。

- **實測結果（修復前）**：**三張表完全沒有開 RLS**。
  - 匿名（anon key，前端公開金鑰）SELECT：三張表皆可讀取全部資料，等同外洩全公司 email 清單與角色配置。
  - 匿名 INSERT：`allowed_emails`、`roles`、`role_permissions` 皆可寫入成功（HTTP 201）——任何拿到 anon key 的人可自行把 email 寫入白名單、繞過整個登入闸门，或竄改角色權限。
  - 測試用的 probe 資料（`rls-probe-delete-me@example.com`、`rls-probe-delete-me` 角色/權限）已用 service_role key 立即清除，未留下垃圾資料。

- **根因確認**：`src/lib/admin.ts`、`src/app/page.tsx`、`src/app/tracker/page.tsx`、`src/app/api/admin/*`、`src/app/api/roles/*` 對這三張表的存取全部經由 `SUPABASE_SERVICE_ROLE_KEY` 建的 client（`getServiceClient()` / 各處手動 `createClient` with service key），沒有任何路徑用 anon/authenticated client 碰這三張表。因此鎖死 RLS 且不加 policy 對現有功能零影響。

- **修復動作**：
  1. 新增 `_開發檔案/sql/step31-lock-allowed-emails-rls.sql`（`ALTER TABLE ... ENABLE ROW LEVEL SECURITY`，三張表皆不加任何 policy）。
  2. 使用者授權後，透過瀏覽器（Claude in Chrome）登入 Supabase Dashboard SQL Editor（`ntapfguwmuufnlafroxs` 專案）執行該腳本，回應 `Success. No rows returned`。
  3. 修復後重新用相同 REST API 手法驗證：匿名 SELECT 三張表皆回傳 `[]`（HTTP 200，RLS 生效但不算錯誤）；匿名 INSERT 回傳 `HTTP 401 / 42501 new row violates row-level security policy`；service_role 讀取仍正常（`HTTP 200`，確認 bypass RLS 不受影響）。
  4. `npm run build` 重新驗證通過（0 errors）。

- **產出檔案**：`_開發檔案/sql/step31-lock-allowed-emails-rls.sql`（新增）
- **修改的既有檔案**：無（純資料庫 RLS 設定變更，程式碼未動）
- **遇到的問題**：`/sql/new` 網址在 Supabase Dashboard 會重定向回最近一次使用的已存查詢分頁，需手動點「+」開新分頁才不會覆蓋到舊的已存查詢（如「User Groups & Items Migration」）。
- **不滿意的點**：無 Supabase CLI/psql 可用，只能靠 REST API 做黑箱驗證，沒有直接查 `pg_policies` 系統表確認底層細節；但功能面驗證（匿名讀寫皆被擋、service_role 正常）已足夠證明修復生效。
- **待辦（功能實測，尚未執行）**：
  1. 用未在清單裡的外部 Gmail 登入，確認被擋在登入頁（`?error=unauthorized`）。
  2. 把該 Gmail 加入清單、指派角色後重新登入，確認能成功進入。
  3. super_admin 點擊「同步公司帳號」，確認公司網域帳號正確被抓入清單，且非 super_admin 看不到此按鈕。
- **已更新常駐檔**：00_執行紀錄.md

---

## [2026-07-08] equipment-cards | Step 31 後續 | 白名單持續驗證漏洞修復 | ✓ 完成（已 push）

- **背景**：使用者實測第 1、2 項待辦時，發現「用私人 Gmail 登入 → 加入清單可通過」正常，但接著把該 Gmail 從 `allowed_emails` 清單刪除後，**該帳號的瀏覽器分頁沒被強制登出，重新整理後仍能繼續看到料卡**。

- **根因調查**：
  1. `middleware.ts` 只檢查 Supabase session cookie 是否存在，不會反查 `allowed_emails`。
  2. `isEmailAllowedToLogin`（登入白名單闸门）只在 `src/app/auth/callback/route.ts` 的 OAuth code exchange 那一刻呼叫一次，之後不會再重新驗證。
  3. `src/lib/admin.ts` 的 `getUserRoleWithPermissions()`：非公司網域帳號一律回傳 `VIEWER_PERMISSIONS`（`read_active_only`），**不管是否還在 `allowed_emails` 清單上**——這是刻意設計（避免外部帳號的 role 欄位被誤設成高權限角色），但副作用是清單移除這個動作對已登入的外部帳號完全沒有實質效果。

- **CodeGraph blast radius 檢查**（依 CLAUDE.md 強制流程）：
  1. `codegraph_explore`（功能描述）找出 `isEmailAllowedToLogin`、`getUserRoleWithPermissions`、`HomePage` 等相關 symbol。
  2. `codegraph_callers(getUserRoleWithPermissions)` 只回報 3 個呼叫點（`requirePermission`、`AdminUsersPage`、以及一個 import），**漏掉了 `src/app/page.tsx`、`src/app/tracker/page.tsx`、`src/app/admin/roles/page.tsx` 及多個 API routes 的直接呼叫**——確認 codegraph 對這類直接函式呼叫（非 JSX）也有漏報，不只 JSX component。
  3. 改用 `Grep` 交叉驗證，抓出 `getUserRoleWithPermissions`/`isEmailAllowedToLogin`/`isAllowedDomain` 全部 13+ 個呼叫點的完整清單。
  4. 逐一判斷「需改 / 不需改」：
     - **需改**：`src/app/page.tsx`（首頁——外部帳號用 baseline `VIEWER_PERMISSIONS` 就能看到現役料卡，是唯一在清單被移除後仍可持續存取內容的入口）。
     - **不需改**：`src/app/tracker/page.tsx`（已被 `requirePermission('view_tracker')` 擋住，`VIEWER_PERMISSIONS` 不含 `view_tracker`）；`src/app/admin/users/page.tsx`、`src/app/admin/roles/page.tsx`（已被 `requireAdmin`/`requirePermission` 擋住）；其餘 API routes（同樣靠 `requirePermission`/`requireAdmin`/`isAllowedDomain` 天然限制，外部帳號降級後權限不足以呼叫）。

- **修復內容**：
  1. `src/lib/admin.ts` 新增 `assertStillAuthorized(supabase, email)`：非公司網域帳號重新檢查 `isEmailAllowedToLogin`，若已被移出清單就 `signOut()` + `redirect('/login?error=unauthorized')`。
  2. `src/app/page.tsx` 的 `HomePage()` 在 `if (!user) redirect('/login')` 後加一行 `await assertStillAuthorized(supabase, user.email)`。

- **修改的既有檔案**：`src/app/page.tsx`（核心保護元件，已先跟使用者說明並取得同意才動）
- **新增內容**：`src/lib/admin.ts` 新增 1 個 function（非新檔案）
- **build 結果**：✓ 通過（0 errors，僅既有無關 ESLint warning）
- **已 commit + push**：`696f670`，Vercel 應已自動部署
- **待辦（功能實測，尚未執行，需等 Vercel 部署完成）**：
  1. 用私人 Gmail 登入（需先加入清單）→ 確認能看到料卡 → super_admin 將該 Gmail 移出清單 → 該 Gmail 分頁重新整理 → 應被強制登出並導向 `/login?error=unauthorized`。
  2. super_admin 點擊「同步公司帳號」，測試帳號清單見上方條目（`king@eup.com.tw` 等 16 個已登入但不在清單上的公司帳號）。
- **已更新常駐檔**：00_執行紀錄.md、CLAUDE.md「此次任務」「目前進度」

---

## [2026-07-08] equipment-cards | Step 31 後續 | 功能實測（使用者執行）| ✓ 完成

- **測試 1：白名單持續驗證修復**——用私人 Gmail 登入 → super_admin 將其移出 `allowed_emails` → 該 Gmail 分頁重新整理首頁 → **通過**，確實被強制登出並導向 `/login?error=unauthorized`。

- **測試 1 延伸提問**：使用者發現「即使在帳號管理頁面把外部 Gmail 指派其他角色（如 admin），該角色依然不會生效，永遠是一般使用者權限」，詢問這是否為合理設計。
  - 確認程式碼：`src/lib/admin.ts` 第 70-71 行 `getUserRoleWithPermissions()` 與第 114 行 `getUserRole()`，只要 email 網域不在 `ALLOWED_DOMAINS`（`eup.com.tw`、`eup.net.vn`），不論 `allowed_emails.role` 存的值為何，一律強制回傳 `VIEWER_PERMISSIONS`（`getUserRole()` 甚至直接回傳 `null`）。
  - 結論：此為刻意設計，非漏洞——已於 Step 31 後續（白名單持續驗證漏洞修復）條目中記載此邏輯的用意（避免外部帳號的 role 欄位被誤設成高權限角色）。
  - 發現一個非漏洞的體驗細節：帳號管理頁面的角色下拉選單目前**沒有限制**外部帳號選其他角色，也沒有提示「選了也不會生效」，屬靜默無效。已告知使用者，暫不處理，之後有需要可另開 Step 讓 UI 鎖定外部帳號角色欄位顯示。

- **測試 2：同步公司帳號功能**——super_admin 點擊「同步公司帳號」，16 個已登入但不在清單上的公司網域帳號皆正確被抓進清單（角色顯示「一般使用者」）；4 個外部 Gmail 皆未被誤抓；非 super_admin 帳號登入帳號管理頁確認看不到「同步公司帳號」按鈕。**通過**。

- **結論**：Step 31 兩項高風險修復（RLS 鎖表、白名單持續驗證漏洞）皆已完成並實測驗證通過，功能穩定可用。
- **已更新常駐檔**：00_執行紀錄.md、CLAUDE.md「此次任務」「目前進度」

---

## [2026-07-08] equipment-cards | 常駐檔狀態校正 | Step 10 / 11 / 16 過時狀態查證 | ✓ 完成

- **背景**：使用者對「Step 16 Phase 2 待照片提供」的說法提出質疑，記得 Step 10、11、16 應該早就做完了，要求查 commit 紀錄確認後更新常駐檔。

- **查證方式**（全程唯讀）：
  1. `git log --oneline --all -- src/types/equipment.ts`：找出各 Step 對應的 commit。
  2. `Grep` 確認 `caption` 欄位已在 `src/types/equipment.ts:17`（Step 11 已落地的直接證據）。
  3. 找 `_開發檔案/scripts/` 與 git log 是否存在批次淨重照片上傳腳本。
  4. 檢查 `_管理/設備料卡完整清單.csv`（2026-06-21 匯出快照）的「淨重kg」欄位實際填寫比例。

- **查證結果**：
  - Step 10（時間戳記顯示 + 最後更新人）：commit `69c423b`，✅ 已完成。
  - Step 9 + Step 11（分類更新 + 細節圖 caption）：commit `c026b3e`，✅ 已完成。
  - Step 16（ESG 淨重欄位、淨重照片、批次匯入 upsert）：commit `9ba80cb`（2026-05-23）；UX 補強（淨重多張照片等）：commit `07fe7d4`（同日）。✅ 功能完成。
  - Step 16「Phase 2」原規劃是「等淨重照片提供後跑批次照片上傳腳本」——`_開發檔案/scripts/` 與 git log 中都找不到這樣的腳本，代表這件事從未被執行，但同時也不是「卡住」，因為：
    - `_管理/設備料卡完整清單.csv`（839 列快照）顯示 **770 列「淨重kg」已有值**，證實淨重數值早已透過 Step 16 Phase 1 就支援的 CSV 批次匯入「淨重」欄位大量回填完成。
    - 該匯出格式沒有「淨重照片」欄位（照片本來就只能經網站 UI 逐筆上傳，無法 CSV 批次帶入），所以「淨重照片」是否補齊無法從 CSV/git 判斷，但這只是資料維護的可選待辦，不影響功能完整性。

- **結論**：Step 10、11、16 皆已完成，常駐檔的舊表格（`00_專案概覽.md` 第 38-49 行）當初建立於 2026-05-23 需求討論當下，後續完成時沒有回頭更新狀態欄位，造成落差。已就地修正狀態欄位，不影響其餘內容。

- **修改的常駐檔**：
  - `_管理/01_equipment-cards/00_專案概覽.md`：Step 10、11、16 狀態改為 ✅ 完成，附上對應 commit hash
  - `CLAUDE.md`：「目前進度」移除「Step 16 Phase 2（批次淨重照片上傳，等照片提供後）」待執行項，改為補充說明該項目已完成、非阻塞待辦
- **修改的既有程式碼檔案**：無（純文件校正）
- **不滿意的點**：淨重「照片」是否所有卡片都已補齊無法單靠 git/CSV 100% 確認，僅能確認數值資料與功能程式碼皆完成；若使用者需要精確的照片覆蓋率，需另外查 Cloudinary 或 DB `weight_photo` 欄位的非空數量
- **已更新常駐檔**：00_執行紀錄.md、CLAUDE.md、00_專案概覽.md

---

## [2026-07-08] equipment-cards | Step 30 階段1 | 完成

- **執行內容**：文件（規格書）管理架構重整——資料正規化 + Google Drive Service Account，接續 plan 檔案 `C:\Users\EupUser\.claude\plans\merry-imagining-pumpkin.md`
  - 委派 `data` agent：新增 `documents` 主檔 + `card_documents` 多對多對照表（RLS 唯讀開放，寫入走 service_role），`equipment_cards.documents` JSONB 欄位角色改為唯讀快取；新增 API routes（`POST /api/documents/upload`、`GET /api/documents/search`、`POST`+`DELETE /api/documents/[id]/link`、`PATCH /api/documents/[id]`），全部掛 `requirePermission('edit_card_documents')`；新增共用邏輯 `src/lib/documents.ts`（重算卡片文件快取）、`src/lib/googleDrive.ts`（Service Account Drive client）
  - 執行前用 CodeGraph + Grep 交叉驗證 `requirePermission` blast radius：CodeGraph 只回報 1 個 caller，Grep 找到 24 個檔案有用到（已知盲點），以 Grep 為準確認全部維持原樣未動
  - `googleapis`／`google-auth-library` 從 devDependencies 移到 dependencies；`googleapis` 內建打包的 `google-auth-library`（10.5.0）與獨立安裝版本（10.7.0）並存導致型別衝突，加 `overrides` 鎖單一版本解決
  - `getDriveClient()` 改為優先讀 `GOOGLE_SERVICE_ACCOUNT_JSON` 環境變數（Vercel 用），沒設定才 fallback 讀本機 `_開發檔案/service-account.json`（因為該檔案不進版控，Vercel 上不存在）
  - 主 session 直接處理（非委派）：`.env.local` 加 `GOOGLE_DRIVE_FOLDER_ID`（使用者提供共用雲端硬碟「設備料卡規格書」→「產品規格書 (上傳)」資料夾 ID）；用 `vercel env add` 把 `GOOGLE_SERVICE_ACCOUNT_JSON`、`GOOGLE_DRIVE_FOLDER_ID` 加進 Vercel Production 環境變數（過程中發現 `.vercel/project.json` 指向的舊 project ID 已失效，`vercel link` 重新連結修正）
- **新增檔案**：
  - `_開發檔案/sql/step30-documents-normalize.sql`
  - `src/app/api/documents/upload/route.ts`
  - `src/app/api/documents/search/route.ts`
  - `src/app/api/documents/[id]/route.ts`
  - `src/app/api/documents/[id]/link/route.ts`
  - `src/lib/documents.ts`
  - `src/lib/googleDrive.ts`
- **修改的既有檔案**：`package.json`、`package-lock.json`、`.gitignore`（service-account.json／.env* 排除，屬階段0已做但先前未 commit）
- **build 結果**：✓ 通過
- **遇到的問題**：正式 Supabase 尚未執行 `step30-documents-normalize.sql`，`documents`/`card_documents` 表還不存在（已用 service role key 直接查證，PGRST205 table not found）
- **不滿意的點**：SQL 檔內的一次性遷移邏輯只能人工執行一次，沒有自動防重跑機制，但兩個 INSERT 都用 `ON CONFLICT DO NOTHING`，重跑不會產生重複資料，風險可接受
- **下一步建議**：階段 2（先執行 SQL，再改寫 `_開發檔案/scripts/upload-spec-books.py` 直接寫入新表，含 `--dry-run`）→ 階段 3（`frontend` agent 改 `CardFormDialog.tsx`）→ 階段 4（`npm run build`、`tester`、`reviewer`）
- **已更新常駐檔**：00_執行紀錄.md、CLAUDE.md、00_專案概覽.md、plan 檔案 `merry-imagining-pumpkin.md`

---

## [2026-07-09] equipment-cards | Step 30 階段2 | ✓ 完成（發現前次未記錄的既有進度 + 修正資料問題）

- **背景**：接續 plan 檔案 `merry-imagining-pumpkin.md` 執行階段 2。開場查證後發現：**階段 2 的核心工作（SQL 遷移 + `upload-spec-books.py` 改寫）在前一個 session 已經做完，只是 context 中斷沒來得及回頭更新常駐檔**，常駐檔記錄的「尚未執行」是過時資訊。

- **查證方式**（主 session 直接處理，唯讀）：
  1. 用 service role key 直接查正式 Supabase：`documents` 表已有 172 筆、`card_documents` 已有 172 筆關聯，且 172 筆 `drive_file_id` 全數正確擷取（0 筆 fallback），證實 `step30-documents-normalize.sql` 已執行過。
  2. 讀 `_開發檔案/scripts/upload-spec-books.py`：內容已是改寫後版本（直接寫入 `documents`/`card_documents` 兩表，不再輸出 `spec-book-links.json`）。
  3. 用 `--dry-run` 實測腳本，結果與正式 DB 現狀精確吃合（172 筆判定已存在跳過），驗證改寫邏輯正確。

- **發現的資料問題**：`--dry-run` 列出 12 個「待上傳新檔案」（分佈於 5 個資料夾：EDR-168P 土石方環保車機、HS DVR(停產)、R007T、XC-43吋壁掛顯示看板、streama Easy Check）。查證這 5 個資料夾在 `_管理/A++規格書料號對照表.csv` 對應的料號，**全部在正式 `equipment_cards` 表不存在**：
  - EDR-168P 土石方環保車機：CSV 記 `1000177`（不存在），實際現役料號應為 `1000174`
  - HS DVR(停產)：CSV 記 `1000044`（不存在），且用品名查完全找不到現役卡片，資料夾本身標「停產」
  - R007T：CSV 記 `1000006`（不存在），且此名稱在系統中對應 4 個不同型號變體，無法判斷該掛哪一個
  - XC-43吋壁掛顯示看板：CSV 記 `3000018`（不存在），查無此產品
  - streama Easy Check：CSV 記 `5000038`（不存在），最接近的現役卡片品名不完全一致

- **處理方式**（使用者確認後執行）：
  1. 從 `A++規格書料號對照表.csv` 移除上述 5 行過時/錯誤的料號對應
  2. 把對應的 5 個本機資料夾移到新建歸檔目錄 `_管理/A++產品規格書/_未對應料號(停產或錯誤)/`（保留檔案本體，不刪除，只移出正常上傳流程）
  3. 因為沒有任何東西真正上傳到 Drive 或寫入正式 DB（全程只用 `--dry-run`），這次清理對線上網站零影響

- **執行中發現並修正的自己引入的 bug**：第一次改寫 CSV 時，寫入邏輯對已存在 BOM 的檔案又多加了一個 BOM（造成檔案開頭有兩個 BOM byte sequence），導致 Python `csv.DictReader` 的 header 欄位比對失效，讓所有列的「大類」欄位都解析成空字串——大部分資料夾因為路徑組合恰好還是能命中而沒發現異常，但破壞了「馥鴻DVR」底下巢狀兩層的 7 個 FH 系列資料夾比對（`本機資料夾不存在：7 筆`）。已重新讀取、去除多餘 BOM、只保留一個後重寫，重新 `--dry-run` 驗證恢復正常（87 個有效條目、0 個資料夾遺失、172 筆已存在跳過、0 筆待上傳）。

- **修改的檔案**：
  - `_管理/A++規格書料號對照表.csv`（移除 5 行過時料號對應；修正過程中誤增又修正的雙重 BOM）
  - 移動（非刪除）：5 個本機資料夾 → `_管理/A++產品規格書/_未對應料號(停產或錯誤)/`
- **未修改**：`_開發檔案/scripts/upload-spec-books.py`（前次 session 已改寫完成，本次僅驗證，無需再動）；正式 Supabase（本次全程唯讀查詢 + `--dry-run`，未寫入）
- **最終驗證結果**：`--dry-run` 顯示 87 個有效條目、87 個資料夾全部找到、172 筆判定已存在跳過、0 筆待上傳、0 筆部分符合待人工核對——狀態乾淨，可安心進入階段 3
- **不滿意的點**：CSV 對照表裡其餘還標「🔶 需確認」的條目（本次只處理了其中 3 個牽涉到這 12 個檔案的）未逐一複查，若之後有新規格書要上傳，建議先過一遍其餘「🔶 需確認」條目是否也有料號過時問題
- **下一步建議**：階段 3（委派 `frontend` agent 改 `CardFormDialog.tsx` 文件區塊，見 plan 檔案設計章節）→ 階段 4（`npm run build`、`tester`、`reviewer`）
- **已更新常駐檔**：00_執行紀錄.md、CLAUDE.md、00_專案概覽.md

---

## [2026-07-09] equipment-cards | Step 30 階段3+4 | ✓ 完成（含 code review 4 項修正）

- **執行內容**：階段 3——委派 `frontend` agent 改寫 `CardFormDialog.tsx` 文件連結區塊（自由文字輸入 → 走正規化 API 即時生效）；階段 4——`npm run build` + `tester` + `reviewer`，發現問題後委派修正。

- **委派前 CodeGraph + Grep blast radius 檢查發現的坑**（規格書沒寫，動工前先補進委派指令）：
  1. `documents` state 原本會在 `handleCreate`/`handleUpdate` 存檔時整包送進 `POST /api/cards`／`PATCH /api/cards/[id]`，直接覆寫 `equipment_cards.documents` 快取欄位——若只改 UI 不動這段，用新 API 更新快取後按「儲存」會被舊快照蓋掉。已在委派指令中要求同步移除。
  2. `CardFormDialog` 的 CodeGraph blast radius 回報 2 個呼叫端（`PhotoWall.tsx`、`TrackerClient.tsx`），Grep 交叉驗證確認 `TrackerClient.tsx` 完全沒有引用，屬 CodeGraph 索引過時假訊號，真正呼叫端只有 `PhotoWall.tsx`。

- **Frontend 執行結果**：
  - 新增 `src/hooks/useDocumentUpload.ts`（封裝 upload/search/link/unlink/updateVersion）
  - 改寫 `CardFormDialog.tsx` 文件區塊：上傳新文件（編輯模式即時上傳、新增模式暫存到卡片建立成功後）、挑選既有文件、「也用於 N 個品號」提示、移除（`ConfirmDialog`）、更新版本
  - 移除 `handleCreate`/`handleUpdate` payload 的 `documents` 欄位、移除 line 329 的 changedFields 比對
  - **自行發現的正確性缺口**：既有 API 沒有「依料號查已掛載文件」的功能，只能靠名稱模糊搜尋反查 id，同名文件會誤判——複查後認為是真正的正確性問題（非僅效能），追加委派 `data` agent 新增 `GET /api/documents?equipment_id=`，frontend 再改用這個 API 取代反查邏輯

- **Tester 驗證**：因登入牆需真實 Google OAuth，自動化瀏覽器無法完成登入（`document.cookie` 確認無 session），改用逐行程式碼追蹤驗證 8 個情境 + 2 個邊界情況，全部邏輯正確。發現一個技術債（不阻斷）：新增卡片模式下，「能建卡片」（`requireAdmin`）與「能編輯文件」（`edit_card_documents`）是兩個獨立權限，理論上可能出現「卡片建立成功但文件處理失敗（403）」，已有妥善報錯不會靜默失敗，暫不處理。

- **Reviewer 發現並修正（2 High + 2 Medium）**：
  1. 🔴 High：`POST /api/cards`、`PATCH /api/cards/[id]` 仍接受 client 傳入 `documents` 欄位直接覆寫快取，繞過正規化資料源——已移除兩個 route 的 `documents` 讀寫
  2. 🔴 High：`DELETE /api/documents/[id]/link` 最後一個關聯時，Drive 刪除失敗仍會繼續刪除 DB `documents` 列，造成永久孤兒 Drive 檔案（`drive_file_id` 線索遺失）——已改為 Drive 刪除失敗時保留 DB 列，回應加 `drive_delete_pending` 旗標
  3. 🟡 Medium：`recomputeCardDocumentsCache` 失敗會讓已成功的核心操作（upload/link/unlink）回報 500，使用者依錯誤重試會建重複檔案或撞 409——已包 try/catch 改為 non-fatal（log only）
  4. 🟡 Medium：`handleRemoveDoc` 用前端過期的 `equipment_ids` 快取決定要不要跳確認框，多人協作下可能誤判成「還有其他關聯」而跳過確認直接觸發最後一個關聯的永久刪除——已改為一律跳 `ConfirmDialog`，只依 otherCount 調整措辭，不再依它決定要不要跳確認框
  - 其餘（`CardFormDialog.tsx` 已達 1400+ 行建議拆元件、`requireAdmin()` 語意與 `create_delete_cards` 不一致等既有問題）列為技術債，未修正，避免擴大改動範圍

- **修改的既有檔案**（含核心保護元件）：
  - `src/components/CardFormDialog.tsx`（⚠️ 核心保護元件——plan 檔案已明確授權僅限文件區塊改動）
  - `src/app/api/cards/route.ts`、`src/app/api/cards/[id]/route.ts`（移除 `documents` 欄位讀寫）
  - `src/app/api/documents/[id]/link/route.ts`、`src/app/api/documents/upload/route.ts`（reviewer 修正）
- **新增檔案**：`src/app/api/documents/route.ts`、`src/hooks/useDocumentUpload.ts`
- **build 結果**：✓ 通過（0 error，僅既有無關 ESLint warning）
- **不滿意的點**：
  1. Tester 無法在瀏覽器實測（OAuth 登入牆），功能面仍待使用者手動驗證
  2. 4 項 reviewer 修正完成後，主 session 自行複查程式碼判斷正確性足夠，未重新委派正式 `reviewer` 再跑一輪——這是效率與嚴謹度的取捨判斷
  3. 新增模式權限繞過（tester 發現）列為技術債未修正
- **建議下一步**：
  1. Commit + push 觸發 Vercel 部署
  2. 瀏覽器實測：編輯卡片文件列表顯示、上傳新文件、挑選既有文件掛到第二張卡片確認兩邊 Lightbox 皆可見、移除文件（一般關聯 vs 最後一個關聯兩種情境）、無 `edit_card_documents` 權限角色確認文件區塊唯讀
  3. Step 30 完成後，`_開發檔案/scripts/` 下的舊 Drive 相關檔案（`link-documents.ts`、`update-card-documents.py`、`drive-credentials.json`、`drive-token.json`）依 plan 檔案「舊 Drive 相關檔案的處置」章節，可評估是否封存
- **已更新常駐檔**：00_執行紀錄.md、CLAUDE.md、00_專案概覽.md、plan 檔案 `merry-imagining-pumpkin.md`

---

## [2026-07-13] equipment-cards | Step 30 使用者實測回饋修正（3 項）| ✓ 完成

- **背景**：commit `d2c86a8` push 後，使用者在正式站（Vercel）用真實 Google 帳號實測文件功能，發現並提出 3 點問題，主 session 逐一查證原因、跟使用者對焦決策後執行修正。

- **使用者實測發現**：
  1. 上傳文件後 Drive 上看不出對應料號；上傳同名檔案到不同卡片會建立兩份重複檔案
  2. 移除文件時警示說「Drive 檔案會一併刪除」，但實際 Drive 上檔案還在
  3. 文件的新增/移除等異動跟卡片編輯視窗的「取消」/「儲存」無關，不論按哪個都已經真的執行了

- **查證**：問題 2 主 session 直接查程式碼確認根因——這正是前一輪 review 修正（Drive 刪除失敗保留 DB 列）生效的情境，`CardFormDialog.tsx` 的 `doRemoveDoc` 完全沒檢查 API 回傳的 `document_deleted`/`drive_delete_pending`，只要 API 沒丟錯就一律當作「已完全刪除」，導致使用者誤判。

- **對焦決策**（`AskUserQuestion` 逐項確認）：
  1. 上傳新文件時，若偵測到精確同名既有文件，跳提示讓使用者選擇「仍要上傳新文件」或「改用挑選既有文件」——採用
  2. 前端修正，讓「移除文件」誠實反映 Drive 是否真的刪除成功——採用
  3. 文件異動全面改成比照其他欄位，暫存到按「儲存」才真正生效，「取消」能悔棋（符合 plan 檔案原規格）——採用

- **執行內容**（委派 `frontend` agent，主 session 事先確認 blast radius：`CardFormDialog` 唯一呼叫端仍是 `PhotoWall.tsx`，Props 介面不變，不需要動呼叫端；本次只改 `CardFormDialog.tsx` 一個檔案的邏輯，未新增/修改任何 API route）：
  - 新 state：`pendingRemoveDocIds`（`Set<string>`）、`pendingVersionUpdates`（`Map<string, File>`）、`duplicateDocPrompt`、`driveWarning`
  - `handleDocFileChosen`／`handlePickExistingDoc`：拿掉 `mode === 'create'` 分支，新增/編輯模式統一走 `pendingDocUploads`/`pendingDocLinks` 暫存（不再立即呼叫 API）
  - 選檔後先呼叫 `docApi.search()` 做精確同名比對（自己用 `.toLowerCase()` 過濾，避免模糊搜尋誤觸發），命中則跳提示（借用既有 `ConfirmDialog`，未修改元件本體）
  - `doRemoveDoc`：改為只標記 `pendingRemoveDocIds`，不呼叫 API；新增 `handleUndoRemoveDoc` 可在儲存前復原
  - `handleDocUpdateVersionFile`：改為暫存進 `pendingVersionUpdates`，不呼叫 API
  - `handleUpdate`：PATCH 成功後依序處理 `pendingRemoveDocIds`（unlink，累計 `drive_delete_pending` 次數）→ `pendingVersionUpdates`（updateVersion）→ `pendingDocUploads`/`pendingDocLinks`（upload/link），每步獨立 try/catch，失敗不 `onClose()`；若有 Drive 清除失敗，用獨立 `driveWarning`（警示色非紅色錯誤色）顯示，同樣不自動關閉視窗
  - `useDocumentUpload.ts`：`DocumentUnlinkResult` 新增選填欄位 `drive_delete_pending?: boolean`（既有函式簽章不變）
  - JSX：待移除文件顯示刪除線 + 「復原」按鈕；待更新版本顯示提示文字；暫存項目文案依新增/編輯模式顯示不同措辭（「待建立料卡後上傳」vs「待儲存後上傳」）

- **主 session 複查**：逐行核對整份 diff，確認邏輯正確、`ConfirmDialog` 重用方式恰當（未加 `danger` prop，非破壞性操作不會顯示警告色）、`useDocumentUpload.ts` 型別擴充未破壞既有簽章。`npm run build` 通過（0 error）。**本輪未重新委派 tester/reviewer**——判斷此輪改動未新增 API、無安全性疑慮，純粹是既有暫存模式的套用，主 session 自行複查已足夠嚴謹，改請使用者直接在瀏覽器實測（比程式碼追蹤式的 tester 更有價值）。

- **修改的既有檔案**（含核心保護元件）：`src/components/CardFormDialog.tsx`（⚠️ 核心保護元件，plan 檔案已授權文件區塊改動）、`src/hooks/useDocumentUpload.ts`
- **build 結果**：✓ 通過（0 error，僅既有無關 ESLint warning）
- **不滿意的點**：`pendingVersionUpdates` 只做「顯示提示」沒有像移除一樣加「取消」按鈕（frontend agent 判斷規格沒明確要求，避免過度發揮），若之後使用者覺得需要可以再補
- **建議下一步**：commit + push → 使用者瀏覽器實測這三項調整
- **已更新常駐檔**：00_執行紀錄.md、CLAUDE.md

---

## [2026-07-09～13] equipment-cards | Step 32 | 報價查詢 | ✓ 完成（已上線）

- **背景**：使用者希望在不影響另一位協作者正在進行的 Step 30（文件正規化）的前提下，新增獨立功能。經腦力激盪收斂：報價查詢——品名 + 標準售價 + 主管權限價（可留空），無料號（許多品項如線材、支架沒有正式料號），分類自由文字可由管理員自訂。使用者提供參考表單 `配件報價2023-08.pdf`（供應鏈管理組內部文件），發現實際有兩種售價欄位（標準售價／主管權限價），據此調整規劃。
- **執行環境限制**：本次執行環境（非原生 Claude Code CLI）無法讀取/呼叫本專案 `.claude/agents/` 下的自訂子 Agent（`data`/`frontend`/`tester`/`reviewer`），改由主 session 依相同規範（CodeGraph/Grep blast radius 檢查、核心保護元件最小侵入、build 驗證）直接執行程式碼變更，並額外呼叫一個通用 review agent 做獨立安全性審查（模擬 reviewer 角色）。
- **執行內容**：
  - 新增 `quote_items` 資料表（SQL：`_開發檔案/sql/step32-quote-items.sql`、`step32b-quote-items-sort-order.sql`）。RLS 全鎖不開放 authenticated 讀寫（比對 Step 31 手法），一律經 API + service_role，欄位層級依權限篩選。
  - `AppSettings` 新增 `quoteCategories` 欄位（分類清單，管理員可用既有 `SettingsPopover` 元件新增/編輯，並支援拖拉排序），`/api/settings` 路由擴充支援此 key。
  - 新增 3 個 permission_key：`view_quotes`、`view_quotes_manager_price`、`edit_quotes`，加入 `VALID_PERMISSION_KEYS` 白名單與 `RolesManager.tsx`（新建角色表單 + 編輯角色頁面兩處皆已加入「報價查詢」勾選區塊）。
  - 新增 API：`/api/quotes`（GET/POST）、`/api/quotes/[id]`（PATCH/DELETE）、`/api/quotes/reorder`（PATCH，比照既有 `/api/roles/reorder` 手法）。
  - 新增 `src/components/QuotesClient.tsx`：搜尋（Fuse.js）+ 分類篩選（可拖拉排序）+ 清單顯示（分類自己的欄位標頭、兩欄排版）+ 管理員新增（可一次新增多筆）/編輯/刪除/拖拉排序品項 + 無編輯權限者顯示「請聯繫 Lala」提示。
  - 核心保護元件最小侵入：`src/app/page.tsx`（新增 `getQuoteItems()` 查詢 + 伺服器端依權限過濾 `manager_price`）、`src/components/PhotoWall.tsx`（頂部新增第 2 個分頁「報價查詢」，沿用「任務板」的 mount-once + CSS hide/show 模式，未變動既有分頁邏輯與版面）。
- **獨立安全審查發現並修正的問題**：
  - 🔴 已修正：`POST`/`PATCH /api/quotes` 原本只檢查 `edit_quotes`，未檢查 `view_quotes_manager_price`，導致擁有 `edit_quotes` 但無 `view_quotes_manager_price` 的角色可透過原始 API 請求寫入/竄改 `manager_price`（即使前端已隱藏該欄位輸入框）。
  - 修正時額外發現一個潛在資料遺失風險：若單純依「無權限就 403 拒絕整個請求」修正，會導致這類使用者一編輯既有項目（前端表單的 `manager_price` 因看不到欄位而預設送出 `null`）就會把資料庫裡原有的主管權限價**覆寫成 null**。改為「無該權限時，PATCH 完全忽略 `manager_price` 欄位（不寫入 update 物件，保留原值）；POST 新建一律以 null 起始」，避免資料被靜默清空。
  - 🟡 已修正：角色若只勾 `edit_quotes` 沒勾 `view_quotes`，會看不到「報價查詢」分頁本身（權限形同無效）。調整分頁顯示條件與 SSR 資料查詢條件為 `view_quotes` 或 `edit_quotes` 任一即可。
- **正式環境執行**：透過瀏覽器操作 Supabase SQL Editor（用 Monaco editor API `setValue()` 直接設定內容並讀回驗證，避免打字模擬被編輯器自動更正打亂），依序執行 `step32-quote-items.sql`（建表、預設分類、管理員權限授予）與 `step32b-quote-items-sort-order.sql`（拖拉排序欄位）；用 Node script（`_開發檔案/scripts/import-quote-items.js`，先 dry-run 確認 88 筆分類統計無誤才 `--live` 執行）匯入 PDF 轉錄的 88 筆初始資料。
- **功能實測**：使用者本機（`npm run dev`）以真實帳號登入實測，包含：清單/搜尋/分類篩選畫面、新增品項（單筆＋多筆）、拖拉排序品項與分類、分類管理彈窗拖拉、一般人員視角（`view_quotes`+`view_quotes_manager_price`，無 `edit_quotes`）確認唯讀且看得到兩欄價格、技師角色確認看不到此功能（查證 `role_permissions` 表證實）。過程中一次因主 session 同時跑 `npm run build` 與 `next dev` 共用 `.next` 快取導致 dev server 資源 404，已刪除 `.next` 重啟解決；此後全程改用 `npx tsc --noEmit` 驗證型別，不再對執行中的 dev server 跑 `next build`。
- **build 結果**：✓ 通過（0 errors，僅既有無關 ESLint warning）
- **產出檔案**：
  - 新增：`_開發檔案/sql/step32-quote-items.sql`、`_開發檔案/sql/step32b-quote-items-sort-order.sql`、`_開發檔案/scripts/import-quote-items.js`、`src/app/api/quotes/route.ts`、`src/app/api/quotes/[id]/route.ts`、`src/app/api/quotes/reorder/route.ts`、`src/components/QuotesClient.tsx`、`_管理/01_equipment-cards/specs/step32-quote-lookup.md`
  - 修改：`src/types/equipment.ts`、`src/lib/settings.ts`、`src/app/api/settings/route.ts`、`src/app/api/roles/[id]/permissions/route.ts`、`src/components/RolesManager.tsx`、`src/components/SettingsPopover.tsx`（新增拖拉排序，並鎖死 `statuses[0]` 不可拖動，因全站多處以此判斷現役狀態）、`src/app/page.tsx`（核心保護元件，僅新增 quoteItems 查詢與 prop）、`src/components/PhotoWall.tsx`（核心保護元件，新增第 2 分頁與 QuotesClient 掛載）
- **不滿意的點**：未經過本專案 `tester`/`reviewer` 子 Agent 的獨立審查（環境限制），改以通用 review agent 補上一次安全性審查
- **已更新常駐檔**：00_執行紀錄.md、CLAUDE.md、00_專案概覽.md、specs/step32-quote-lookup.md

---

## [2026-07-13] equipment-cards | Drive 刪除永久失敗根因診斷 + 修正 | ✓ 完成

- **背景**：使用者實測上一輪修正後，儲存時跳出「已儲存，但其中 2 份文件的關聯已解除、Google Drive 檔案清除失敗」的誠實回報（證明上上輪 review 修正 + 這輪前端誠實回報都正常運作），但追問「為什麼會這樣」。這代表 Drive 刪除持續性地失敗，不是單一偶發事件，需要查根因。

- **查證方式**：主 session 直接用本機 `_開發檔案/service-account.json`（跟正式站 Vercel 用的是同一組 Service Account 憑證）寫一次性診斷腳本，對卡住的 2 個檔案分別呼叫 `drive.files.get()` 查 `capabilities`，再實際呼叫 `drive.files.delete()` 重現錯誤。過程中一度因為本機網路連線不穩定（DNS 解析失敗）拿到不可靠的結果，確認網路恢復正常後重跑才拿到乾淨結果。

- **根因確定**：兩個檔案的 `capabilities` 都顯示 `"canTrash": true` 但 `"canDelete": false`。**Service Account 在共用雲端硬碟的「內容管理員」角色只能把檔案移入垃圾桶，沒有「永久刪除」的權限**，但程式碼呼叫的是 `drive.files.delete()`（要求永久刪除權限），所以一律失敗——Google API 對此回傳的是容易誤導的「404 File not found」而非「403 Forbidden」，這也是先前無法從錯誤訊息直接看出根因的原因。這是系統性問題，不是單一檔案的偶發狀況。

- **對焦決策**（`AskUserQuestion`）：提出兩個修法——(A) 改用 Drive 內建垃圾桶（`trashed:true`）(B) 建立實體「_待清除文件」資料夂，改用搬移取代刪除。使用者一開始誤解方案 A 是「建資料夾」，追問釐清後選擇 **方案 B（實體資料夾）**，理由是比垃圾桶更直觀、隨時能自己開來看。

- **執行內容**（主 session 直接處理，未委派，因為範圍小且已完全診斷清楚）：
  1. 用 Service Account 直接呼叫 Drive API，在共用雲端硬碟「設備料卡規格書」根目錄（與既有「產品規格書 (上傳)」同層）建立新資料夾「_待清除文件」（ID `1u38kmLRsM0fD2KHZXQgfJ7vMc1jvcR0C`），並實測搬移前述 2 個卡住的測試檔案進去，確認 Service Account 有搬移權限（`canMoveItemWithinDrive`/`canAddChildren` 相關能力，不受 `canDelete:false` 影響）
  2. `.env.local` 新增 `GOOGLE_DRIVE_PENDING_DELETE_FOLDER_ID`；`npx vercel env add` 補上 Vercel Production 環境變數（`.vercel/project.json` 已連結，`npx vercel whoami` 確認登入身分 `homejay-4810`）
  3. `src/lib/googleDrive.ts` 新增 `DRIVE_PENDING_DELETE_FOLDER_ID` export，附上根因說明註解
  4. `src/app/api/documents/[id]/link/route.ts` 的 DELETE handler：最後一個關聯要清除文件本體時，改為先查該檔案目前實際的 `parents`（不能假設一定是 `DRIVE_FOLDER_ID`——早期透過個人 OAuth 遷移的舊資料可能在不同資料夾），再呼叫 `drive.files.update()` 搬移（`addParents`/`removeParents`）取代 `drive.files.delete()`；搬移失敗時仍維持既有的 `drive_delete_pending` 保護機制（保留 documents 列）
  5. 清理本次測試殘留：2 筆孤兒 `documents` 列（`card_documents` 關聯已於前一輪測試中正確解除，Drive 檔案已人工搬移到新資料夾）直接用 service role key 確認 0 關聯後刪除

- **修改的既有檔案**：`src/lib/googleDrive.ts`、`src/app/api/documents/[id]/link/route.ts`
- **新增檔案**：4 個一次性診斷/設定腳本於 `_開發檔案/scripts/`（`test-drive-delete.js`、`find-drive-structure.js`、`create-pending-delete-folder.js`、`move-to-pending-delete.js`，未進版控，僅本機備查）
- **build 結果**：✓ 通過（0 error）
- **不滿意的點**：新資料夾「_待清除文件」目前完全靠人工定期檢查決定要不要真的刪，沒有自動清理機制或提醒；如果之後文件刪除頻率變高，可考慮之後加一個定期通知或直接把 Service Account 權限提升成「管理員」（但會擴大 Service Account 權限範圍，需重新評估取捨）
- **建議下一步**：commit + push → 使用者再次實測移除文件，確認這次能正常顯示「文件已完全刪除」（`document_deleted: true`），不再跳 Drive 清除失敗警告
- **已更新常駐檔**：00_執行紀錄.md、CLAUDE.md

---

## [2026-07-13] equipment-cards | Step 30 對話收尾交接（context window 55%）| 無程式碼異動

- **背景**：使用者實際使用 Drive 刪除修正後的功能，指出目前文件連結功能只是「堪用」，還沒達到他要的完整使用體驗，同時 context window 已達 55%，主動要求收尾以便無縫接軌下一個 session。本輪純粹是需求範圍釐清 + 常駐檔交接，沒有動任何程式碼。

- **使用者提出的 5 個開放問題**（詳細內容已寫進 `CLAUDE.md`「目前進度」Step 30 區塊，供下一個 session 直接引用）：
  1. 批次上傳（目前一次一個檔案）
  2. 批次變更舊檔／換版（目前逐筆更新版本）
  3. 重複上傳情境（目前只處理單筆精確同名比對，批次/相似命名情境未涵蓋）
  4. 樂觀顯示，減少感覺延遲
  5. Drive 內檔案組織方式（目前平面單一資料夾，要不要依料號分子資料夾）

- **交接安排**：`CLAUDE.md`「此次任務」已寫明下一個 session 要先回到腦力激盪階段逐一討論這 5 點（一次一個問題、列選項優缺點），不要跳過討論直接動手實作。

- **已更新常駐檔**：00_執行紀錄.md、CLAUDE.md（「目前進度」Step 30 區塊 + 「此次任務」）

---

## [2026-07-14] equipment-cards | Step 30b 文件管理功能 | 腦力激盪收斂完成（無程式碼異動）

- **背景**：接續上一輪交接的 5 個開放問題，逐一討論收斂（一次一個問題、列選項優缺點，過程中多次用 `mcp__visualize` 畫示意圖/畫面草稿協助對焦），並延伸出兩個額外決策（頁籤入口位置、權限設計）。全程純討論，沒有動任何程式碼。

- **收斂結果摘要**（完整規格見新建的 `_管理/01_equipment-cards/specs/step30b-document-management.md`）：
  1. **批次上傳**：拆成「單卡片多選」（`CardFormDialog` 比照細節照片模式）與「跨卡片批次上傳」（新頁籤，重用既有已支援 `equipment_ids: string[]` 的 `POST /api/documents/upload`，查證後確認無需新增後端邏輯）
  2. **批次換版**：討論中一度設計成「批次替換版本」一對一配對 UI，後來使用者明確否決（目的是要能整批刪除不留孤兒、方便重傳，不想被迫一對一配對），改為「批次刪除（刪前列出受影響料卡清單）」+「批次上傳」兩個獨立動作組合
  3. **重複上傳情境**：討論中發現現有已上線的「精確同名跳提示」（仍要上傳新文件／改用既有文件）缺少「取代」這個最常見的真實情境（同名＝多半是新版本），確認回頭統一修改為「取代（更新版本）／先刪除舊的再上傳」二選一，移除「保留兩份同名」（避免批次刪除清單出現無法分辨的重複檔名），兩個上傳入口統一
  4. **Drive 檔案組織**：查證確認共用雲端硬碟（Shared Drive）的檔案只能有一個父資料夾，排除「依料號建子資料夾」與「檔名加料號前綴」兩個方向（皆會讓共用文件產生歸屬落差或被迫重複命名），改用新增一份自動產生的 Google Sheet「文件目錄表」（依文件/依料號兩分頁）解決業務端查找歸屬的需求；也排除「按鈕做在 Sheet 裡用 Apps Script」的想法（會脫離主專案的維護/部署/權限體系），按鈕留在新頁籤內
  5. **樂觀顯示**：主動點出使用者自己在「Step 30 使用者實測回饋修正」那輪才修過「移除文件顯示已刪除但實際未刪成功」的教訓，說服使用者不要對 Drive/DB 操作做假樂觀，改為批次逐筆即時進度回饋
  6. **入口位置（延伸決策）**：原規劃「文件管理」連結掛在角色管理頁面裡，使用者看了現有 PhotoWall 分頁截圖後糾正，改為跟「任務板」「人為配件報價」同層級的獨立頁籤，並確認 mount-once + CSS hide/show 的既有模式能天然避免切換頁籤時遺失暫存資料或中斷背景執行中的批次動作
  7. **權限設計（延伸決策）**：新增獨立 permission key `manage_documents`（不重用 `edit_card_documents`，因為批次刪除的影響範圍跟單卡片編輯風險層級不同），三個子功能（批次上傳/批次刪除/重新產生目錄檔）綁同一個總開關，使用者親自指定插入位置（角色管理頁面「料卡管理」分組之後、「追蹤板」分組之前）

- **過程中查證程式碼確認的關鍵事實**（避免規格建立在錯誤假設上）：
  - `POST /api/documents/upload` 的 API 簽章本來就是 `equipment_ids: string[]`，`useDocumentUpload.ts` 的 `upload()` 也已經是這個簽章，只是 `CardFormDialog` 目前呼叫時永遠只傳一個 id——確認跨卡片批次上傳不需要新後端邏輯
  - `PATCH /api/documents/[id]`（更新版本）只覆蓋 Drive 檔案內容與 `updated_at`，不動 `documents.name`/類型欄位，也不影響 `card_documents` 關聯——確認「取代」在結構上不會產生孤兒風險
  - `GET /api/documents` 目前 `equipment_id` 為必填，只能查單一卡片——確認批次刪除/文件清單需要擴充此路由支援「列出全部」模式

- **產出檔案**：`_管理/01_equipment-cards/specs/step30b-document-management.md`（新建，完整設計決策/Permission/API/前端異動/驗收標準）
- **修改的既有檔案**：無（純規劃）
- **不滿意的點**：Google Sheets API 是否已在 Google Cloud 專案啟用尚未查證，規格內已註記為執行階段待確認事項
- **建議下一步**：下一個 session 先跟使用者過一次規格檔案內容確認無誤，再提出步驟結構與執行摘要，取得同意後依序委派 `data` → `frontend` → `tester` → `reviewer`
- **已更新常駐檔**：00_執行紀錄.md、CLAUDE.md（「目前進度」Step 30 區塊 + 「此次任務」）、新建 specs/step30b-document-management.md

---

## [2026-07-14] equipment-cards | Step 32 補丁：供應鏈看不到主管權限價 | ✓ 完成

- **背景**：使用者回報「供應鏈」角色即使勾選「可看主管權限價」，人為配件報價頁面仍看不到該欄位，要求釐清權限邏輯，並強調修改前要用 CodeGraph 避免改A壞B。

- **CodeGraph 調查**：依 CLAUDE.md 強制流程，先用 `codegraph_explore` 追完整條鏈路（`RolesManager.tsx` 勾選 UI → `PUT /api/roles/[id]/permissions` 儲存 → `getUserRoleWithPermissions()` 讀取 → `page.tsx`/`PhotoWall.tsx`/`QuotesClient.tsx`/`GET /api/quotes` 消費），確認整個「人為配件報價」權限機制**本來就是純 permission-key 驅動、不綁定任何角色名稱**，已符合使用者「依角色彈性勾選、不綁死特定角色」的最終目的，架構面沒有問題。
  - `codegraph_callers(RolesManager)` 回報「無呼叫者」，屬於已知的 JSX 呼叫盲點（CLAUDE.md 已記載此限制）；改用 `Grep` 交叉驗證，確認唯一呼叫端是 `src/app/admin/roles/page.tsx`，未受本次改動影響。

- **根因確認**（直接查正式 Supabase，用暫存 service-role 腳本查完即刪）：「供應鏈」角色的 `role_permissions` 只存了 `view_quotes_manager_price`，**沒有存 `view_quotes`（也沒有 `edit_quotes`）**。而 `page.tsx`/`PhotoWall.tsx` 判斷整個「人為配件報價」分頁要不要顯示，用的是 `permissions.includes('view_quotes') || permissions.includes('edit_quotes')`——沒有這兩個 key 之一，`quoteItems` 直接是空陣列、分頁本身都不會顯示，不是「看得到分頁但缺一欄」。不是資料錯誤（無重複角色、`allowed_emails.role` 皆精確比對正確），是管理員當初勾選時漏勾了父權限 `view_quotes`。

- **UI 修正**（使用者確認採用「加連動」方案，自行到角色管理頁補勾資料，不需程式碼補資料）：
  - `src/components/RolesManager.tsx` 新增 `handleQuoteViewToggle`／`handleQuoteManagerPriceToggle` 兩個函式，讓「可看主管權限價」與「可看人為配件報價」父子連動：勾主管權限價時自動一併勾可看報價；取消可看報價時，主管權限價也一併取消。`edit_quotes`（新增/編輯報價品項與價格）維持獨立勾選，不受影響。

- **修改的既有檔案**：`src/components/RolesManager.tsx`（僅此一檔）
- **build 結果**：✓ 通過（0 errors, 0 warnings）
- **驗證方式**：`/admin/roles` 有 Google OAuth 登入牆，preview 瀏覽器無法直接驗證登入後畫面（沿用 Step 30 已知限制），改以 build 通過 + CodeGraph/Grep 程式碼追蹤驗證邏輯正確
- **不滿意的點**：無
- **使用者實測**：重新勾選「供應鏈」的「可看人為配件報價」+「可看主管權限價」並儲存後，正式站畫面正常顯示。**通過**。
- **已 commit + push**：`3df8207` → `origin/main`，Vercel 應已自動部署
- **已更新常駐檔**：00_執行紀錄.md

---

## [2026-07-14] equipment-cards | 帳號管理頁：加入日期 → 初始登入/最後登入 | ✓ 完成（待使用者實測）

- **背景**：使用者對焦帳號管理頁「加入日期」欄位語意（實際是 `allowed_emails.created_at`，即被加入白名單的時間，非登入時間），希望改顯示 Supabase `auth.users` 的 `created_at`（初始登入）與 `last_sign_in_at`（最後登入）。已先跟使用者釐清 `updated_at` 不可靠（session refresh 也會變動），以及「登入次數／使用時長」Supabase 無內建資料、需另外設計記錄機制，使用者選擇本次只做顯示欄位，後續再另外討論使用統計方案。

- **CodeGraph 調查**：`codegraph_explore` 確認 `fetchAllowedEmails`（`src/app/admin/users/page.tsx`）只有 1 個呼叫端、`UserRow`（`UserManagementTable.tsx`）只在該檔案內部使用，blast radius 小。額外查到 `sync-users` route 已有 `service.auth.admin.listUsers({ page, perPage: 1000 })` 分頁撈全部 Auth 使用者的既有寫法，直接比照複用。

- **執行內容**（主 session 直接處理，未委派，2 個既有檔案、範圍小）：
  1. `src/app/admin/users/page.tsx`：新增 `fetchAuthTimestamps()`，分頁呼叫 `service.auth.admin.listUsers()`，用 email（小寫）建 `auth_created_at`/`last_sign_in_at` map；`users` 改為 `fetchAllowedEmails()` 結果 merge 這個 map 後的陣列，查無對應 Auth 帳號時兩欄皆為 `null`
  2. `src/components/UserManagementTable.tsx`：`UserRow` 新增 `auth_created_at`/`last_sign_in_at`（皆為選填 `string | null`）；`formatDate()` 補 null 保護，顯示「尚未登入」；表格欄位「加入日期」拆成「初始登入」「最後登入」兩欄
- **邊界情況**：手動新增 Email（`handleAdd`）、同步公司帳號（`syncUsers`）當下 client 端組出的新列還沒有 Auth 時間戳，會先顯示「尚未登入」，重新整理頁面後才補上正確值（不影響既有新增/同步功能）
- **修改的既有檔案**：`src/app/admin/users/page.tsx`、`src/components/UserManagementTable.tsx`
- **build 結果**：✓ 通過（0 errors）
- **驗證方式**：此頁面有 Google OAuth 登入牆，沿用 Step 30/32 已知限制，改以 build 通過 + CodeGraph/Grep 追蹤程式碼邏輯驗證，未做瀏覽器實測
- **不滿意的點**：無
- **待辦**：使用者登入正式站 `/admin/users` 確認「初始登入」「最後登入」兩欄顯示正確
- **下一步討論（使用者已表示後續要談）**：登入次數／使用時長／落地率統計——Supabase Auth 沒有內建欄位，需自建記錄機制（例如登入事件寫入自訂資料表），屬於另一個獨立規模的功能，尚未設計
- **已更新常駐檔**：00_執行紀錄.md

---

## [2026-07-14] equipment-cards | 帳號管理頁：同步/新增當下誤顯示「尚未登入」修正 | ✓ 完成

- **背景**：使用者點「同步公司帳號」後，發現剛同步進來的帳號（照定義一定是已在 Supabase Auth 登入過才會被抓到）畫面上卻顯示「尚未登入」，質疑邏輯有問題。實查後確認邏輯本身沒錯，是顯示時機的落差——上一輪執行紀錄已在「邊界情況」預先記載這個已知缺口，這次使用者剛好踩到。

- **根因**：`同步公司帳號`／`新增 Email` 兩個動作，前端在 API 回應後是直接在瀏覽器端組一筆新的資料塞進畫面（optimistic update），但當時的 API 回應只有 `email`/`role`/`created_at`（`allowed_emails` 表的欄位），沒有一併帶回 Auth 的 `created_at`/`last_sign_in_at`，導致這兩欄暫時性顯示「尚未登入」，要等重新整理頁面（重新走 `page.tsx` 的完整查詢）才會更新成正確時間。

- **修正內容**：
  1. `src/app/api/admin/sync-users/route.ts`：既有的 `service.auth.admin.listUsers()` 分頁迴圈直接順便建立 email → 登入時間戳的 map（原本只挑出 email 做網域過濾），回傳 `added` 清單時 merge 這個 map，讓新同步進來的帳號一次就帶正確時間
  2. `src/app/api/admin/users/route.ts`：POST 新增 email 成功後，新增 `findAuthTimestamps(email)` 輔助函式（分頁掃 Auth 使用者清單找符合的 email，因 Admin API 沒有直接依 email 查詢的方法），回應多帶 `auth_created_at`/`last_sign_in_at`
  3. `src/components/UserManagementTable.tsx`：`handleAdd` 從 POST 回應讀取這兩個新欄位塞進本地新增的那一列（`syncUsers` 因為原本就是把整個 API 回應的 `added` 陣列直接當 `UserRow[]` 用，不需要改）
- **修改的既有檔案**：`src/app/api/admin/sync-users/route.ts`、`src/app/api/admin/users/route.ts`、`src/components/UserManagementTable.tsx`
- **build 結果**：✓ 通過（0 errors）
- **驗證方式**：沿用登入牆限制，build + 程式碼追蹤驗證；未做瀏覽器實測
- **不滿意的點**：`findAuthTimestamps` 每次手動新增 email 都要重新分頁掃一次 Auth 清單（目前僅 37 筆使用者，成本可忽略；若公司規模成長到數千人以上，可考慮改用 Supabase 的 email filter API 或快取）
- **建議下一步**：使用者實測「同步公司帳號」「手動新增 Email」，確認新加入的帳號若原本已登入過，畫面能一次顯示正確的初始/最後登入時間，不再閃一次「尚未登入」
- **已更新常駐檔**：00_執行紀錄.md

---

## [2026-07-14] equipment-cards | Step 30b 文件管理功能 | ✓ 完成（build 通過，待執行 SQL + 瀏覽器實測）

- **背景**：接續同日稍早收斂完成的規格 `_管理/01_equipment-cards/specs/step30b-document-management.md`，使用者確認執行。依序委派 `data` → `frontend` → `tester` → `reviewer`，過程中共 3 輪回頭修正。

- **執行內容**：
  1. **`data` agent（後端）**：`GET /api/documents` 擴充為三種模式（`equipment_id` 既有不變／`document_id` 新增單筆反查／都不帶列出全部）；新增 `POST /api/documents/regenerate-index`（查 `documents`/`card_documents`/`equipment_cards`，寫入 Google Sheet「文件目錄表」兩分頁，file id 存 `app_settings`）；`VALID_PERMISSION_KEYS` 加入 `manage_documents`；`googleDrive.ts` 新增 `getSheetsClient()`/`DRIVE_ROOT_FOLDER_ID`。查證確認 Google Sheets API 已啟用、共用雲端硬碟根目錄 ID 為 `0ANjIxrBkz_eeUk9PVA`。
  2. **`frontend` agent（前端）**：`CardFormDialog.tsx`（核心保護元件，規格已授權）文件上傳改多選、顯示名稱固定用檔名、同名二選一改為「取代／先刪除再上傳」；新建 `DocumentsClient.tsx`（重新產生目錄檔＋批次上傳＋文件清單/批次刪除）；`PhotoWall.tsx`（核心保護元件）新增「文件管理」頁籤（mount-once 模式）；`RolesManager.tsx` 新增「文件管理」權限分組。
  3. **主 session 直接處理**：`.env.local` 與 Vercel Production 新增 `GOOGLE_DRIVE_ROOT_FOLDER_ID`。
  4. **回頭修正 1**（frontend agent 自行發現，主 session 追加委派）：`CardFormDialog.tsx` 的「先刪除舊的再上傳」原本靠 `listAll()`（需 `manage_documents`）查受影響料卡，一般編輯者（只有 `edit_card_documents`）會 403 退化成只顯示張數——追加委派 `data` agent 新增 `GET /api/documents?document_id=` 窄範圍反查（權限降為 `edit_card_documents`），`frontend` agent 接上；同時補上 `CardFormDialog.tsx` 多選上傳的本批次內查重（`pendingDocUploadsRef`）。
  5. **`tester` 驗證**：因登入牆改用程式碼逐行追蹤，抓到 1 個確認的 bug——`DocumentsClient.tsx` 批次上傳的本批次內查重因 closure 讀到過期 `rows` state 實際失效，已請 `frontend` agent 比照 `CardFormDialog.tsx` 的 `ref` 模式（新增 `rowsRef`）修正。
  6. **`reviewer` 審查**：發現 1 個 Critical 邊界情況（`CardFormDialog.tsx` 的 `pendingRemoveDocIds`／`pendingVersionUpdates` 沒有互斥，先「更新版本」後反悔「移除」時，順序會導致誤導錯誤訊息，極端情況甚至覆蓋其他料卡共用的文件內容），因不確定是否在規格授權範圍內只回報未直接修——主 session 判斷修法明確、風險小，追加委派 `frontend` agent 修正（新增 `clearPendingVersionUpdate` 輔助函式）；另直接修正 1 個 Medium（`DocumentsClient.tsx` 批次刪除單一料卡 unlink 失敗會整個中斷且不回報，改為逐張獨立 try/catch + 新增 `deleteErrors` 錯誤橫幅）。

- **新增檔案**：
  - `src/app/api/documents/regenerate-index/route.ts`
  - `src/components/DocumentsClient.tsx`
  - `_開發檔案/sql/step30b-manage-documents-permission.sql`（尚未在正式 Supabase 執行）
  - `_管理/01_equipment-cards/specs/step30b-document-management.md`

- **修改的既有檔案**：
  - `src/app/api/documents/route.ts`、`src/app/api/roles/[id]/permissions/route.ts`、`src/hooks/useDocumentUpload.ts`、`src/lib/googleDrive.ts`
  - `src/components/CardFormDialog.tsx`（**核心保護元件**，規格已授權範圍：多選上傳/同名二選一/批次內查重/pending 移除與版本更新互斥）
  - `src/components/PhotoWall.tsx`（**核心保護元件**，規格已授權範圍：新增「文件管理」頁籤，未動既有分頁邏輯與版面）
  - `src/components/RolesManager.tsx`（新增「文件管理」權限分組）
  - `.env.local`（新增 `GOOGLE_DRIVE_ROOT_FOLDER_ID`，未 commit）

- **build 結果**：✓ 通過（0 errors，僅既有無關 ESLint warning），全程共重跑 6 次確認

- **⚠️ 發現工作目錄裡混有另一個未提交的獨立功能（Step 33 使用統計）**：`git status` 顯示 `src/components/CardDetailDialog.tsx`、`src/components/PhotoWall.tsx`、`src/components/RolesManager.tsx`、`src/components/UserMenu.tsx` 同時存在 Step 33（`_管理/01_equipment-cards/specs/step33-usage-analytics.md`，新增 `AnalyticsClient.tsx`/`useHeartbeat.ts`/`src/lib/analytics*.ts`/`src/app/admin/analytics/`/`src/app/api/*/analytics/` 等）的未提交改動，跟 Step 30b 的改動交錯在同一批檔案裡（尤其 `PhotoWall.tsx`、`RolesManager.tsx` 兩個檔案本身同時含有兩個 Step 的變更）。本次執行全程未觸碰 Step 33 的邏輯，但因為改動交錯在同一個檔案內，**git commit 時無法乾淨地只包含 Step 30b、排除 Step 33**，需要使用者決定處理方式（一起 commit／等 Step 33 那邊完成再一起處理／其他方式），故本輪尚未執行 commit。

- **不滿意的點**：
  1. 上述 Step 33 交錯問題，導致無法自主決定是否 commit
  2. Tester 提到「重新產生目錄檔」的「上次產生時間」目前只在單次瀏覽器 session 內有效，重新整理頁面後歸零顯示，若要跨 session 顯示需要另外補一個讀取 `app_settings.documentIndexSheet` 的 GET 端點（未修，列技術債）
  3. Reviewer 列的技術債未修：`regenerate-index` 兩個分頁用 `Promise.all` 平行寫入，其中一頁失敗會留下不一致狀態；`DocumentsClient.tsx` 單檔 623 行偏大，建議之後拆成 3 個子元件
  4. 全程因登入牆限制，`tester`/`reviewer` 都只能程式碼追蹤驗證，未做真實瀏覽器操作

- **建議下一步**：
  1. 使用者決定 Step 33 交錯檔案的處理方式後才能 commit + push
  2. 執行 `_開發檔案/sql/step30b-manage-documents-permission.sql`（管理員角色預設授予 `manage_documents`）
  3. 部署後瀏覽器實測：`CardFormDialog` 多選上傳、同名二選一（含「先刪除再上傳」列出受影響料卡）、「文件管理」頁籤（批次上傳/批次刪除/重新產生目錄檔）、非 `manage_documents` 角色看不到頁籤
  4. 確認 Vercel Production 環境變數已同步 `GOOGLE_DRIVE_ROOT_FOLDER_ID`（本機已加，`npx vercel env add` 已執行）

- **已更新常駐檔**：00_執行紀錄.md、CLAUDE.md（「目前進度」Step 30 區塊 + 「此次任務」）

---

## [2026-07-14] equipment-cards | Step 33 使用統計 | ✓ 完成（build/型別通過，待 SQL + commit + 瀏覽器實測）

- **背景**：接續帳號管理頁「初始登入/最後登入」的討論，使用者希望進一步掌握登入次數、單次停留時長、關鍵功能使用情況（判斷系統有沒有落地）。腦力激盪逐一收斂：追蹤範圍選「完整使用行為分析」、關鍵指標以料卡檢索/瀏覽為主、查看權限用獨立 permission key（`view_analytics`，非綁死 super_admin）、心跳頻率 60 秒。規格見 `_管理/01_equipment-cards/specs/step33-usage-analytics.md`。

- **執行順序**：`data` → `frontend` → 主 session 修正 tester 抓到的缺漏 → `reviewer` → 主 session 修正 reviewer 抓到的高風險問題。

- **`data` agent（後端）**：新增 `login_events`／`usage_sessions`／`usage_events` 三張表（`_開發檔案/sql/step33-usage-analytics.sql`，皆 `ENABLE ROW LEVEL SECURITY` 不加 policy，比照 Step 31/32 鎖表手法，尚未在正式 Supabase 執行）；新增 `POST /api/analytics/heartbeat`、`POST /api/analytics/event`（email 皆從伺服器端 session 取得）；新增 `src/lib/analytics.ts` 的 `getUsageAnalyticsSummary()` 彙總函式（獨立函式設計，方便前端 SSR 直接 import 或走 API 都可以）；`VALID_PERMISSION_KEYS` 加入 `view_analytics`。CodeGraph + Grep 確認該白名單只有這一處定義，無需同步其他地方。

- **`frontend` agent（前端）**：新增 `src/lib/analyticsClient.ts`（client 端 `logUsageEvent`/`getOrCreateSessionId`，刻意跟後端 `src/lib/analytics.ts` 分開檔名避免衝突）、`src/hooks/useHeartbeat.ts`（60 秒心跳，`document.hidden` 時暫停）、`src/app/admin/analytics/page.tsx` + `AnalyticsClient.tsx`（`requirePermission('view_analytics')` 保護）；`PhotoWall.tsx` 掛 `useHeartbeat()` + 800ms debounce 記錄 `card_search`；`CardDetailDialog.tsx` 記錄 `card_detail_view`；`RolesManager.tsx` 新增「使用統計」權限勾選；`UserMenu.tsx` 新增入口連結。過程中發現並修正一個必要的既有小 bug：`PhotoWall.tsx` 呼叫 `<UserMenu email={userEmail} />` 完全沒傳 `permissions`，導致 `UserMenu` 內建的任務板連結邏輯一直是死碼——補上 `permissions={permissions}` 讓「使用統計」連結（以及連帶的既有「任務板」連結）才能正常顯示，判斷為必要且低風險的資料流修正，特別記錄避免被誤認為未授權的行為變更。

- **`tester` 驗證**（因登入牆改用逐行程式碼追蹤，共驗證 9 項）：8 項通過，抓到 1 個 High——`login_events` 表從未被寫入（`data` agent 漏做規格明確要求的 `auth/callback/route.ts` insert），導致「登入次數」對所有人永遠顯示 0。主 session 直接修正（`auth/callback/route.ts` 在 `isEmailAllowedToLogin` 通過後、redirect 前，用 service role client fire-and-forget insert 一筆，try/catch 不擋登入流程），`npx tsc --noEmit` 通過。

- **`reviewer` 審查**：抓到 1 個 High——`POST /api/analytics/heartbeat` 的 update/insert 判斷只用 `session_id` 查詢，沒有比對 email，共用電腦情境（分頁沒關、換帳號登入，`sessionStorage` 的 session id 會沿用）會把心跳誤更新到別人名下，造成停留時長資料被灌水/漏記。主 session 直接修正兩處：①`heartbeat/route.ts` 的查詢加 `.eq('email', user.email)`，insert 遇到 PK 衝突（id 屬於別人）直接吞掉錯誤（fire-and-forget，不影響任何邏輯）；②`analyticsClient.ts` 新增 `clearSessionId()`，`UserMenu.tsx` 的 `signOut()` 登出時呼叫，從源頭避免下一位登入者沿用同一 session id。`npx tsc --noEmit` 通過。
  - Medium/Low 列為技術債未修（reviewer 建議可視工作量決定）：`metadata`/`event_type` 沒有長度上限、心跳/事件 API 沒有 rate limit、service client 建立方式在多檔重複、`GET /api/admin/analytics` 目前是死碼（`AnalyticsClient` 只吃 SSR 傳入的 `initialData`，沒有 client fetch 這支 API，非錯誤只是重複）

- **⚠️ 工作目錄現況（跟 Step 30b 交錯）**：`PhotoWall.tsx`、`RolesManager.tsx`、`src/app/api/roles/[id]/permissions/route.ts` 這 3 個檔案同時混有另一個 session 的 Step 30b（文件管理）未提交改動，本次全程只碰 Step 33 相關的部分，未觸碰 Step 30b 邏輯。已跟使用者確認 Step 30b 是別的 session 在做、目前已跑完，但兩邊改動在共用檔案裡糾在一起（`RolesManager.tsx` 甚至被 Step 30b 重構過權限勾選區塊的共用函式），無法用逐行 patch 安全切開成兩筆獨立 commit，commit 策略待使用者下指示時再處理。`CardDetailDialog.tsx`、`UserMenu.tsx`、`src/app/auth/callback/route.ts`、`src/lib/analyticsClient.ts`、`src/app/api/analytics/heartbeat/route.ts` 等純 Step 33 檔案已確認乾淨無交錯。

- **修改的既有檔案**（純 Step 33 部分）：`src/app/auth/callback/route.ts`、`src/components/CardDetailDialog.tsx`、`src/components/UserMenu.tsx`、`src/app/api/analytics/heartbeat/route.ts`；以及與 Step 30b 交錯、僅使用統計相關片段是本次改動：`src/components/PhotoWall.tsx`、`src/components/RolesManager.tsx`、`src/app/api/roles/[id]/permissions/route.ts`
- **新增檔案**：`_開發檔案/sql/step33-usage-analytics.sql`、`src/app/api/analytics/heartbeat/route.ts`、`src/app/api/analytics/event/route.ts`、`src/app/api/admin/analytics/route.ts`、`src/lib/analytics.ts`、`src/lib/analyticsClient.ts`、`src/hooks/useHeartbeat.ts`、`src/app/admin/analytics/page.tsx`、`src/components/AnalyticsClient.tsx`、`_管理/01_equipment-cards/specs/step33-usage-analytics.md`
- **驗證方式**：`npx tsc --noEmit` 通過（tester 另外跑過一次 `npm run build` 也通過，唯一警告與 Step 33 無關）；全程因登入牆限制，未做真實瀏覽器操作
- **不滿意的點**：
  1. Step 30b/33 檔案交錯導致無法自主決定 commit 策略，待使用者下指示
  2. Phase 2 事件（任務板/報價查詢/文件的使用記錄）本次未做，規格已預留架構，之後只需多加呼叫點
  3. Medium/Low 技術債（見上）未修
- **建議下一步**：
  1. 使用者決定 Step 30b/33 交錯檔案的 commit 策略
  2. 使用者手動在正式 Supabase 執行 `_開發檔案/sql/step33-usage-analytics.sql`
  3. 部署後瀏覽器實測：`/admin/analytics` 頁面顯示、心跳是否正常運作、`card_search`/`card_detail_view` 事件是否被正確記錄、`view_analytics` 權限勾選是否生效
- **已更新常駐檔**：00_執行紀錄.md

---

## [2026-07-15] equipment-cards | Step 30b 第二輪優化（使用者實測回饋）| ✓ 完成

- **背景**：使用者實測第一版文件管理功能後，逐一提出 6 項體驗回饋（一次一個問題討論收斂，過程見對話紀錄，無獨立規格文件，直接以待辦清單追蹤）：同名文件「取代」提示不夠清楚、受影響料卡清單擠成一行難讀、確認框沒有捲軸、文件目錄表 Google Sheet 機制決定拿掉改用純前端匯出 CSV、批次上傳的料卡選擇器沒顯示完整清單/文件類型不能彈性增減、文件清單希望改成依文件/依料號雙視圖並支援展開/新增/取消掛載。依序委派 `data`（後端清理）→ `frontend`（前端大改版）→ `tester` → `reviewer`，過程中 1 輪回頭修正。

- **執行內容**：
  1. **`data` agent**：移除「重新產生目錄檔」後端機制——刪除 `src/app/api/documents/regenerate-index/route.ts`，`src/lib/googleDrive.ts` 移除 `getSheetsClient()`/`DRIVE_ROOT_FOLDER_ID`/`spreadsheets` scope。Grep 確認乾淨無殘留呼叫端（僅前端 `useDocumentUpload.ts` 待同步清除）。
  2. **`frontend` agent**：
     - `CardFormDialog.tsx`（核心保護元件，僅動兩個 `ConfirmDialog`）：「發現同名文件」補上「取代（更新版本）會連同其他掛載此文件的料號一起更新」提示；「這份文件也掛載在其他料卡」改用 `ConfirmDialog` 既有但先前沒用到的 `detail` prop（本來就 `max-h-44 overflow-y-auto` 可捲動），清單改一行一個料卡
     - `DocumentsClient.tsx` 從 623 行精簡到約 71 行，拆成 `src/components/documents/` 底下 6 個子元件（`EquipmentPicker`、`EquipmentQuickPick`、`BatchUploadPanel`、`AddDocumentToCard`、`SortHeader`、`ExpandableDocumentList`）
     - 移除目錄表 Google Sheet 區塊，改成純前端 CSV 匯出（`Blob` + UTF-8 BOM，依當下視圖匯出）
     - 文件清單改為依文件/依料號雙視圖：`GET /api/documents` 既有回傳的 `linked_cards` 純前端反向分組（不需新 API），支援排序、展開/摺疊、新增掛載（`EquipmentQuickPick`/`AddDocumentToCard`）、取消掛載（沿用「最後一個關聯會整個刪除文件」保護），批次選取/刪除維持不變
     - `EquipmentPicker` 補上「共 N 張料卡，顯示前 50 筆」提示；`BatchUploadPanel` 補上 `SettingsPopover`（`documentTypes`）齒輪按鈕，比照 `CardFormDialog.tsx` 既有用法
  3. **`tester` 驗證**：程式碼追蹤逐項確認 6 項回饋皆正確實作，發現 1 個 Medium——`ExpandableDocumentList.tsx` 兩個表格用固定像素欄寬、外層沒包 `overflow-x-auto`，手機窄螢幕會橫向溢出（專案其他表格皆有此既有慣例），已請 `frontend` agent 修正（加 `overflow-x-auto` + `min-w` 包裹，不動欄寬本身）。
  4. **`reviewer` 審查**：整體通過，直接修正 2 項——① CSV 匯出的 `toCsv()` 補上公式注入防護（檔名/品名以 `=`/`+`/`-`/`@` 開頭時加單引號強制當文字，避免 Excel 開啟時被當公式執行）；②`RolesManager.tsx` 的 `manage_documents` 權限說明文字仍寫著已移除的「重新產生目錄檔」，改成現有能力描述。另回報 2 項非阻塞：手機版 `overflow-x-auto` 容器內有絕對定位下拉選單（`EquipmentQuickPick`），理論上可能與既有「shadcn/ui Popover 在 overflow:hidden 父容器需改 fixed 定位」同類問題撞在一起，建議部署後實機驗證；`manage_documents`／`edit_card_documents` 是兩個獨立權限、彼此不隱含，若角色只給前者會出現「看得到清單但點什麼都 403」的體驗落差，建議之後角色設定兩者一起給。

- **新增檔案**：`src/components/documents/{EquipmentPicker,EquipmentQuickPick,BatchUploadPanel,AddDocumentToCard,SortHeader,ExpandableDocumentList}.tsx`
- **刪除檔案**：`src/app/api/documents/regenerate-index/route.ts`
- **修改的既有檔案**：`src/components/CardFormDialog.tsx`（核心保護元件，僅兩個 ConfirmDialog）、`src/components/DocumentsClient.tsx`（精簡為組裝層）、`src/hooks/useDocumentUpload.ts`（移除 `regenerateIndex()`）、`src/lib/googleDrive.ts`（移除 Sheets 相關）、`src/components/RolesManager.tsx`（權限說明文字更新，reviewer 直接修正）
- **build 結果**：✓ 通過（0 errors，僅既有無關 ESLint warning），全程共重跑 4 次確認
- **不滿意的點**：
  1. `ExpandableDocumentList.tsx` 480 行，reviewer 列為技術債建議之後再拆（純展示表格/link-unlink 邏輯抽 hook）
  2. 手機版下拉選單巢狀捲動疑慮未實機驗證（登入牆限制，全程僅程式碼追蹤 + build）
  3. `manage_documents`/`edit_card_documents` 權限落差是既有架構、非本輪引入，記為技術債
- **建議下一步**：
  1. 部署後瀏覽器/手機實測：CSV 匯出中文與公式注入防護、依文件/依料號雙視圖操作、`EquipmentQuickPick` 下拉選單在手機展開最後一列時是否正常浮動
  2. 角色設定 `manage_documents` 時記得同時給 `edit_card_documents`，避免權限落差
- **已更新常駐檔**：00_執行紀錄.md、CLAUDE.md（目前進度 Step 30 區塊）
- **Step 30b/33 檔案交錯的 commit 策略**：已於下一輪確認——使用者指出這個問題其實在另一個 session 已經處理過（commit `6bb4750`／`8a6f0cb`／`6f0ca3a` 依序：Step 30b 第一版、Step 33、整合共用檔案），本條目「待決定」的敘述是主 session 對話當下資訊過時所致，非真的卡住。

---

## [2026-07-15] equipment-cards | Step 30b 第三輪優化：批次新增/取消掛載 | ✓ 完成

- **背景**：使用者部署後實測第二輪成果，反映「新增掛載料卡」「挑選既有文件」這兩個挑選器是單選、選一次要重開一次下拉；「取消掛載」每點一筆就立刻執行且會重新整理整份清單，移除多筆要重複等待。先與使用者對焦確認需求（範圍：依文件、依料號兩個視圖都要改），並用 CodeGraph + Grep 確認 `EquipmentQuickPick`/`AddDocumentToCard` 只被 `ExpandableDocumentList.tsx` 使用、`ExpandableDocumentList` 只被 `DocumentsClient.tsx` 使用（CodeGraph 對 `ExpandableDocumentList` 這個 JSX 呼叫沒抓到，Grep 補上確認），確認影響範圍侷限在這 3 個檔案，才委派執行。

- **執行內容**（僅 `frontend` → `tester` → `reviewer`，本輪範圍小未動後端）：
  1. `EquipmentQuickPick.tsx`：新增掛載料卡從單選點擊立即生效，改成打勾複選＋「確認新增（已選 N）」按鈕，`onPick` 改為 `onPickMany`
  2. `AddDocumentToCard.tsx`：挑選既有文件同樣改複選（用 `Map` 保留已選文件，換關鍵字重新搜尋不遺失勾選），`onPickExisting` 改為 `onPickManyExisting`；上傳新文件分支不動
  3. `ExpandableDocumentList.tsx`：兩視圖「✕ 取消掛載」改成 checkbox＋「批次取消掛載（已選 N）」，新增 `computeUnlinkPlan()` 依最新完整快照判斷這批裡哪些會導致文件整個被刪除、哪些只是解除關聯，確認框分兩段列出；批次序列（非平行）呼叫 `unlink()`，失敗收集不擋其他筆，全部跑完只呼叫一次 `onChanged()`（解決「按一筆等很久」）

- **`tester` 驗證**：程式碼追蹤逐項確認正常路徑、混合情境（同批同時有「會刪除」跟「只解除關聯」）、失敗容錯、`busyIds` 鎖定、`stopPropagation` 皆正確，僅列 3 項非阻塞技術債（下拉關閉不清空選取、批次刪除與批次取消掛載無互斥鎖、未逐筆回報 `drive_delete_pending`）。

- **`reviewer` 審查，發現並直接修正 1 個 High + 1 個 Medium**：
  - 🔴 High：`computeUnlinkPlan()` 用來判斷「會不會整個刪除」的 `documents` 快照可能在使用者操作期間過期（例如另一分頁同時異動了同一份文件的掛載關係），導致誤判成「只是解除關聯」而不顯示警告，但後端其實即時查真實狀態、文件本體真的被刪除了，使用者卻只看到溫和提示。修正：批次送出後逐一比對 `unlink()` 實際回傳的 `document_deleted`，若「預期只是解除關聯、實際卻被刪除」，額外跳警示列出這些文件名提醒使用者去確認。
  - 🟡 Medium：依料號視圖的 `AddDocumentToCard` 沒有在批次取消掛載執行期間鎖定（`busyIds` 只鎖了 checkbox 跟依文件視圖的 `EquipmentQuickPick`），補上 `disabled` prop。

- **修改的既有檔案**：`src/components/documents/{EquipmentQuickPick,AddDocumentToCard,ExpandableDocumentList}.tsx`（皆非核心保護元件）
- **build 結果**：✓ 通過（0 errors，僅既有無關 ESLint warning），全程共重跑 3 次確認
- **不滿意的點**：「快照過期誤判」目前是事後偵測＋警示，不是事前阻止（考量 10-50 人內部系統、`manage_documents` 權限把關同時操作的人本來就少，reviewer 判斷這個權衡合理，先不做事前重新 fetch）；`ExpandableDocumentList.tsx` 持續增長，之後有需要可再拆
- **建議下一步**：部署後實機驗證批次新增/取消掛載操作是否順暢；確認 Vercel 已部署最新版本
- **已更新常駐檔**：00_執行紀錄.md

---

## [2026-07-20] equipment-cards | Step 34 | 設備套餐（部門共享群組） | ✓ 完成

- **背景**：使用者提出「我的關注」群組功能是個人私有的，想要同部門共用、可指定跨部門可見的群組功能。經多輪對焦（獨立頁面 vs 原地整合、可見度模型、編輯權限細緻度、批次分享/掛載、複製套餐、我的關注↔套餐重複維護疑慮）收斂出完整規格，寫入 `_管理/01_equipment-cards/specs/step34-equipment-packages.md`。

- **執行內容**（`data`→`frontend`→`tester`→`reviewer`標準順序）：
  1. **`data`**：新建 `equipment_packages`/`package_items`/`package_shared_departments` 三張表（SQL：`_開發檔案/sql/step34-equipment-packages.sql`）、`user_groups` 補 `updated_at` 欄位、10 支 `/api/packages/*` route、`src/lib/departments.ts` 共用函式（抽出 `getCallerDepartmentId`）、`VALID_PERMISSION_KEYS` 加 4 個新 key
  2. **`frontend`**：`/packages` 頁面、`PackagesClient.tsx`、`usePackages.ts`、`src/components/packages/` 系列元件（依套餐/依料號雙視圖、清單/照片切換、批次操作、分享部門彈窗、複製套餐彈窗）、`GroupsPanel.tsx` 加對齊/複製按鈕（規格允許範圍內）、`PhotoWall.tsx` 只加入口連結、`RolesManager.tsx` 加套餐權限分組
  3. **`tester`**：程式碼追蹤驗證部門隔離（含管理員無例外）、名稱唯一性、權限隱含邏輯、批次操作全批拒絕、對齊機制擁有者驗證、內容重複偵測範圍，皆通過，無發現問題
  4. **`reviewer`**：抓到 2 個 🔴必須修正（`batch/share` 先刪後插非原子操作、`source_group_id` 併發競態無 DB 層防護）+ 6 個 🟡建議修正（GET 權限檢查重複查詢、`page.tsx` 未複用共用函式、`PackageExplorer.tsx` 過大、批次取消掛載/掛載未走批次端點、批次陣列無長度上限），已由 `data`/`frontend` 平行修正（`data` 順手修了一個無關但擋 build 的 `for...of over Map` 型別問題），主 session 手動讀過修正後的 diff 覆核確認正確

- **主 session 額外把關**：發現這個 worktree 從未 `codegraph init`，委派 prompt 也未明確要求子 Agent 走 CLAUDE.md 的 CodeGraph 強制流程；事後補建索引並用 `codegraph_explore` + `git diff` + `Grep` 自行做了一次 blast radius 檢查，確認 `frontend` 越界修改的 4 支既有群組 API route（補寫 `user_groups.updated_at`）沒有遺漏的呼叫端、`PhotoWall.tsx`/`GroupsPanel.tsx`/`RolesManager.tsx` 的改動都是加法性質未破壞既有邏輯

- **正式環境部署**：主 session 用瀏覽器登入正式 Supabase Dashboard（專案 `Equipment Card` / `ntapfguwmuufnlafroxs`），取得使用者當場確認後執行 SQL migration，並用查詢驗證 3 張新表與管理員 4 個新權限皆正確建立

- **新增檔案**：見規格文件與各階段委派內容，主要為 `src/app/api/packages/**`、`src/app/packages/**`、`src/components/packages/**`、`src/hooks/usePackages.ts`、`src/lib/departments.ts`、`_開發檔案/sql/step34-equipment-packages.sql`
- **修改的既有檔案**：`src/app/api/groups/{[id]/route,[id]/items/route,[id]/items/[equipmentId]/route,replace/route}.ts`（僅補 `updated_at` 更新）、`src/components/{GroupsPanel,PhotoWall,RolesManager}.tsx`、`src/types/equipment.ts`、`src/app/api/roles/[id]/permissions/route.ts`
- **遇到的問題**：兩次子 Agent 因 session 用量上限中止，皆恢復完成；一次流程疏漏（委派時未明確要求 CodeGraph 強制流程），事後補做验证
- **build 結果**：✓ 通過，全程共重跑數次確認
- **不滿意的點**：無
- **建議下一步**：使用者實機驗證「其他部門分享給我的套餐」FK join 語法與名稱/來源群組唯一性衝突提示是否符合預期；手機版排版待實機截圖驗證
- **已更新常駐檔**：CLAUDE.md「目前進度」、00_執行紀錄.md

---

## [2026-07-21] equipment-cards | 私人信箱角色生效 + 使用者清單排序 + 文件工具列 RWD | ✓ 完成

- **背景**：使用者三項需求。① 私人 Gmail（如 `jay10001228@gmail.com`，代表外包人員）能登入但指派的角色不生效（指了業務卻看不到人為配件報價）；② 帳號管理與使用統計兩張使用者清單要能排序；③ 文件管理工具列在手機上跑版（依文件/依料號、匯出CSV、批次刪除全折行）。先與使用者對焦確認需求，並用 CodeGraph + Grep 查 blast radius 後才動手。

- **根因確認（第①項）**：`admin.ts` 的 `getUserRoleWithPermissions()`（第 71 行）對任何非公司網域信箱**硬回傳 `VIEWER_PERMISSIONS`**，不管 `allowed_emails` 指派了什麼角色；`getUserRole()`（第 114 行）同款邏輯但查無 caller（死碼）。Grep 確認 `getUserRoleWithPermissions` 被 9 處呼叫（page/tracker/roles/quotes/issues/cards/settings…），移除網域降級後角色會在所有地方一致生效。登入閘門 `isEmailAllowedToLogin` 已保證非公司信箱須先在 `allowed_emails` 才進得來，故拿掉降級不會放進未授權外人。`api/admin/*`、`roles/assignable` 仍保留 `isAllowedDomain` 閘門（管理類操作維持公司信箱限定，更安全，不動）。

- **RWD 方案**：先用 mockup 給使用者看三種手機收法（A圖示化+橫向捲／B分兩層／C搜尋獨立一行），使用者選 B 案。

- **執行內容（主 session 直接改，未委派——4 檔、皆非核心保護元件）**：
  1. `src/lib/admin.ts`：移除 `getUserRoleWithPermissions`／`getUserRole` 兩處網域降級，改以 `allowed_emails` 指派角色決定權限，未指派者仍 fallback 到 viewer
  2. `src/components/UserManagementTable.tsx`：Email／角色／初始登入／最後登入 四欄可點擊升降序（時間 null「尚未登入」恆排最後）
  3. `src/components/AnalyticsClient.tsx`：六欄皆可排序（改為 client 排序，預設維持後端總停留時長 desc）
  4. `src/components/documents/ExpandableDocumentList.tsx`：工具列 `flex-col sm:flex-row`，手機分兩層（標題+切換／搜尋+匯出+批次刪除，搜尋撐滿），桌機單排不變

- **驗證**：`npm run build` 編譯+型別通過；4 檔繞過巢狀 eslintrc 跑 `eslint --no-eslintrc` = 0 errors（唯一 warning 在 `UserManagementTable.tsx:87` 的既有下拉定位 effect，非本次引入）。第①項純程式碼、`jay10001228` 已是業務，部署後重新整理即生效，無需改資料。

- **commit/push**：`945ab9e`，已 `git push origin HEAD:main`（`c75e14c..945ab9e`），Vercel 自動部署中。
- **建議下一步**：部署後實測——① `jay10001228@gmail.com` 重新整理後應能看到人為配件報價；② 兩張清單排序；③ 手機看文件工具列不再折行。
- **已更新常駐檔**：00_執行紀錄.md

---

## [2026-07-20] equipment-cards | Step 34 使用者實機測試後第二輪優化 | ✓ 完成

- **背景**：Step 34 設備套餐上線後，使用者實機操作回報 4 點使用回饋，另外順帶回報 1 個不相關既有系統的 bug。先用 `Explore` agent + 主 session 親自開瀏覽器上正式站實測，釐清每點的實際現況（尤其第 3 點「照片模式批次刪除」一開始描述模糊，實測後才確認是「展開套餐後料卡的批次取消掛載」而非「套餐本身變成縮圖卡片」），全部對焦收斂完才委派執行。

- **執行內容（分兩批分開 commit）**：
  1. **主 session 直接處理**（單檔小改動、不委派）：`src/components/PhotoWall.tsx:248-259` 次級篩選改用精確 `includes` 比對取代 Fuse.js 模糊搜尋，修正「MDVR-4G-2鏡」會誤篩選出「MDVR-4G-8鏡」的 bug（跟 `CLAUDE.md` 已知技術債「Fuse.js 純數字查詢要走 includes」同類問題，這次是混合英數字標籤）。
  2. **委派 `frontend`**（prompt 中明確要求走 CodeGraph blast radius 流程）：
     - `/packages` 從獨立路由改為內嵌分頁，比照任務板/人為配件報價/文件管理的 `activeTab` + mount-once 模式：`src/app/page.tsx` 新增 `getPackagesData()`、`PhotoWall.tsx` 新增 `packagesMounted` state 與渲染區塊、`src/app/packages/` 整個資料夾刪除、`PackagesClient.tsx` 搬到 `src/components/` 底下改吃 props
     - `GroupsPanel.tsx` 群組標題列 hover 編輯按鈕從 `absolute` 改成 flex sibling（`opacity-0 pointer-events-none` → hover 才 `opacity-100 pointer-events-auto`），修正跟對齊狀態徽章重疊
     - `PackageListView.tsx` 照片/網格模式補上跟清單模式共用同一份 `selectedUnlinkKeys` 的批次取消掛載
     - `PackageExplorer.tsx` 新增分享成功 toast（`setTimeout` ref 在 unmount 時正確清除）+ 分享對象標籤即時同步（等 `onChanged()` 完成才顯示 toast，非樂觀更新）

- **`tester` 驗證**：程式碼追蹤逐項確認權限判斷一致性、`allCards` 沒有重複查詢、`/packages` 舊路由無殘留引用、按鈕重疊修正保留 hover 互動、批次選取狀態跨清單/照片模式共用、toast 部門數量計算取用戶最終確認結果，皆通過無問題。

- **`reviewer` 審查**：僅 2 個 🟡 建議（`PackagesClient.tsx` 搬移後殘留未使用的 `userEmail` prop、toast 缺 `aria-live`/`role="status"` 無障礙屬性），無 🔴 必須修正項。主 session 直接修正這 2 點（移除 prop + `PhotoWall.tsx` 對應呼叫端同步移除、toast div 加 `role="status" aria-live="polite"`），修正後重新跑 build + 單獨 eslint 確認無誤。

- **修改的既有檔案**：`src/app/page.tsx`、`src/components/{PhotoWall,GroupsPanel}.tsx`（皆核心保護元件，事前已跟使用者明確確認範圍）、`src/components/packages/{PackageExplorer,PackageListView}.tsx`
- **新增/刪除**：`src/components/PackagesClient.tsx`（從 `src/app/packages/PackagesClient.tsx` 搬移）；`src/app/packages/`（整個資料夾刪除）
- **build 結果**：✓ 通過，兩批改動皆額外跑過針對性 `eslint --no-eslintrc` 確認無誤（worktree 巢狀 eslintrc 衝突已知盲點，主 session 於 main checkout 重新驗證一次）
- **commit/push**：兩批分開 commit + merge + push main——次級篩選修正 `2fabc02`（merge 後 main `4db46cc`）、套餐優化 `7b382f0`（merge 後 main `205ad20`）
- **不滿意的點**：無
- **建議下一步**：使用者實機驗證分頁切換是否真的無刷新感、GroupsPanel 按鈕不再重疊、照片模式批次取消掛載、分享 toast 提示、次級篩選精確度
- **已更新常駐檔**：CLAUDE.md「目前進度」、00_執行紀錄.md

---

## [2026-07-20] equipment-cards | 第三輪優化：主搜尋精確度＋次級篩選拖拉排序 | ✓ 完成

- **背景**：使用者實機測試套餐第二輪成果後，回報 2 點跟套餐無關的既有系統問題：① 主搜尋列模糊搜尋範圍太寬（例如搜「MDVR-4G-4鏡」誤搜出「HS 4G-DVR」等完全不相關的結果）；② 次級篩選標籤要能自由拖拉調整順序。

- **問題 1 對焦過程**：先用 `Explore` agent 查明根因——`PhotoWall.tsx` 的 Fuse.js 設定對 7 個欄位一起加權模糊比對，只要任一欄位分數夠低就整體入選，不要求查詢的每個片段都對得上；且主搜尋列是全專案唯一用到 `fuse.search()` 做非結構化查詢比對的地方（其他分頁各自獨立）。列出 3 個方案（A. 精確比對優先+模糊當備援／B. 多詞 AND 限縮／C. 單純調緊 threshold 參數）附優缺點讓使用者選，選定方案 A。

- **問題 2 對焦過程**：確認 `category_subfilter_tags` 表已有 `sort_order` 欄位、PUT API 本就是整包覆蓋儲存陣列順序（不需改資料庫/API），並找到 `QuotesClient.tsx` 現成的分類拖拉排序實作（原生 HTML5 drag events，無第三方套件）可直接比照複製寫法。

- **執行內容（委派 `frontend`，prompt 中明確要求走 CodeGraph blast radius）**：
  1. `PhotoWall.tsx` 的 `filtered` useMemo 非數字查詢分支改兩階段：先用 `includes()` 比對 `equipment_id`/`name`/`tags`（`.some()`）/`vendor`/`category`，有精確結果就只用精確結果（不疊加模糊結果），完全沒結果才 fallback 回原本的 `fuse.search()`；純數字查詢分支、`fuse` 實例建立、次級篩選標籤自己的精確比對邏輯皆未被觸碰
  2. `SubfilterTagBar.tsx` 編輯區塊比照 `QuotesClient.tsx` 的 `handleCategoryReorder` 寫法新增拖拉排序，純本地 state 操作（不觸發 API），使用者按「儲存」時沿用既有 `handleSave()` 送出新順序

- **`tester` 驗證**：程式碼追蹤確認兩階段搜尋優先順序正確（精確有結果就不再跑模糊）、`tags` 陣列比對語意正確、純數字/`selectedSubTags`/`fuse` 實例三處確認未被改動、拖拉排序不誤觸 API、拖曳 state 正確清空，邊界情況（空字串、大小寫）皆符合預期。抓到 1 個非阻塞小建議（`onDrop` 缺 `e.preventDefault()`），主 session 直接修正。

- **`reviewer` 審查**：效能（786+ 筆資料下兩階段搜尋 + Fuse fallback 皆無疑慮）、`fuse` fallback 路徑確實可達（非死碼）、整體風格一致皆通過。抓到 2 個非阻塞建議：① 搜尋結果文案仍寫死「模糊搜尋」，精確命中時用詞不準確；② `handleTagReorder` 用 `indexOf(tag)` 若未來出現重複標籤字串會抓錯拖曳位置。主 session 直接修正：文案改中性用詞「搜尋「{query}」」；拖拉排序的 `draggingTag`/`dragOverTag`（字串）改成 `draggingIndex`/`dragOverIndex`（數字），用 index 記錄來源/目標位置，從根本避免依賴標籤字串唯一性假設。

- **修改的既有檔案**：`src/components/{PhotoWall,SubfilterTagBar}.tsx`
- **build 結果**：✓ 通過，額外針對兩個檔案跑獨立 `eslint --no-eslintrc` 確認無誤；main checkout 合併後重新驗證 build 一次同樣通過
- **commit/push**：`6136ff0`（feature 分支），merge 後 main `c507d9b`
- **不滿意的點**：無
- **建議下一步**：使用者實機驗證搜尋精確度（例如「MDVR-4G-4鏡」不再誤搜出不相關結果）、次級篩選標籤拖拉排序操作是否順暢
- **已更新常駐檔**：CLAUDE.md「目前進度」、00_執行紀錄.md

---

## [2026-07-20] equipment-cards | 分頁內嵌化副作用修正：設備套餐切回不更新 | ✓ 完成

- **背景**：第二輪優化把設備套餐從獨立路由改成內嵌分頁（首次進入才 mount，之後 CSS 隱藏保留 state）後，使用者實測發現在「我的關注」把群組複製成套餐後，切到設備套餐分頁看不到剛複製的結果，要整頁重新整理才會出現。

- **原因**：套餐資料只在該分頁**第一次** mount 時抓一次，之後單純靠 `activeTab` 切換 CSS `hidden`，不會重新 mount，也就不會重新抓資料。

- **對焦過程**：列出 2 個方案讓使用者選——A. 每次切回套餐分頁就重抓一次（簡單、涵蓋所有讓套餐資料變舊的情境）／B. 跨元件即時通知（複雜，且只覆蓋這一種情境）。使用者選 A。

- **執行內容（單檔小改動，主 session 直接處理、未委派）**：
  - `src/components/PackagesClient.tsx`：新增 `isActive: boolean` prop，新增 `useEffect(() => { if (!isActive) return; if (canViewOwn) refreshOwn(); if (canViewShared) refreshShared() }, [isActive])`，每次這個分頁被切回（`isActive` 由 false 變 true）就重新抓一次本部門+分享套餐清單
  - `src/components/PhotoWall.tsx`：`&lt;PackagesClient&gt;` 呼叫端新增 `isActive={activeTab === 'packages'}`

- **build 結果**：✓ 通過，額外跑獨立 `eslint --no-eslintrc` 確認無誤；main checkout 合併後重新驗證 build 一次同樣通過
- **commit/push**：`011f5fa`（feature 分支），merge 後 main `660dbbd`
- **不滿意的點**：無
- **建議下一步**：使用者實機驗證「我的關注」複製/對齊套餐後切到設備套餐分頁是否立即看到最新結果
- **已更新常駐檔**：CLAUDE.md「目前進度」、00_執行紀錄.md

---

## [2026-08-11] equipment-cards | 我的關注個人群組新增「複製群組」功能 | ✓ 完成

- **背景**：使用者反映「我的關注」頁面遇到很多內容相似的群組（套餐），沒辦法在頁面上複製一個去修改，只能一直手動新建再逐張加料卡。跟使用者對焦 3 個設計方向（A. 沿用既有「複製套餐」互動模式／B. 新增群組時選來源複製／C. 多選料卡存成新群組）並列出優缺點，使用者選定方案 A（學習成本低、改動範圍小）。

- **CodeGraph 調查**：worktree 缺 `.codegraph/` 索引，先 `codegraph init` 建立後查詢，確認 `GroupsPanel.tsx` 是唯一入口、`handleCopyToPackage`/`handleAddGroup` 皆只有自己內部呼叫；Grep 交叉確認全專案只有 `PhotoWall.tsx` 引用 `<GroupsPanel>`，改動不影響任何核心保護元件。

- **執行內容（`data`＋`frontend` 平行委派）**：
  1. 新增 `src/app/api/groups/[id]/duplicate/route.ts`（POST）：驗證登入與來源群組屬於本人（不符或不存在統一回 404，不洩漏差異）；複製名稱＋`group_items` 建立完全獨立的新群組（不建立來源關聯）；名稱重複（23505）回 409「群組名稱已存在」
  2. `GroupsPanel.tsx` 新增 `DuplicateGroupDialog` inline 彈窗（比照既有 `DuplicatePackageDialog.tsx` 互動模式，預設帶「原名稱（副本）」）、`duplicateTarget` state、`handleConfirmDuplicate` handler；群組標題列 hover 操作按鈕列新增「複製群組」圖示（不受 `canManagePackages` 限制，一般使用者皆可用；預設群組不顯示）

- **`tester` 驗證**：程式碼追蹤確認未登入/空名稱/名稱重複/複製他人群組皆正確擋下，複製後兩個群組的 `group_items` 是完全獨立的 row、互不影響；build 與獨立 `eslint --no-eslintrc` 皆通過。

- **`reviewer` 審查**：安全性（無 IDOR，`user_id` 全部來自 session）、edge case、既有慣例一致性皆通過。抓到 1 個非阻塞建議：re-fetch 新群組失敗時的 fallback（`full ?? newGroup`）會缺少 `group_items` 欄位，可能讓前端渲染炸掉——主 session 直接修正為 `full ?? { ...newGroup, group_items: [] }`。

- **技術債（記錄不處理）**：
  - `duplicate/route.ts` 建立新群組與插入 `group_items` 是分兩次獨立呼叫，中間沒有交易保護，若群組建立成功但料卡插入失敗會留下沒有料卡的孤兒群組。這跟既有 `groups/route.ts` GET 的懶遷移邏輯（先建群組再從 `user_bookmarks` 插入 `group_items`）是同一種既有架構模式，非本次新增風險，且修法（引入 RPC/transaction 或孤兒群組清理機制）屬專案層級決定，影響範圍超出這兩個檔案，暫不處理。
  - `DuplicateGroupDialog`（`GroupsPanel.tsx`）與 `src/components/packages/DuplicatePackageDialog.tsx` 幾乎逐行相同，只有文案「群組/套餐」不同，屬合理的重複邏輯，可考慮未來抽成共用元件，非必要。

- **修改/新增的檔案**：`src/app/api/groups/[id]/duplicate/route.ts`（新增）、`src/components/GroupsPanel.tsx`
- **build 結果**：✓ 通過（worktree 內因缺 `.env.local` 出現 `/api/subfilter-tags` 既有無關錯誤；main checkout 合併後重新驗證 build 一次乾淨通過，確認該錯誤純屬 worktree 環境問題）
- **commit/push**：`67eb5c8`（feature 分支 `claude/group-duplication-feature-463628`），merge 後 main `4f3313d`
- **不滿意的點**：無
- **建議下一步**：使用者實機驗證複製群組功能（複製後新群組是否帶有跟來源一樣的料卡清單，且能獨立編輯不影響原群組）
- **已更新常駐檔**：00_執行紀錄.md（本次未更新 CLAUDE.md「目前進度」，範圍依使用者指示僅限執行紀錄）

---

## [2026-08-11] equipment-cards | 複製群組圖示改用 FolderPlus | ✓ 完成

- **背景**：使用者實機看到「複製為套餐」跟「複製群組」兩個按鈕用同一個 `Copy` 圖示，分不清點的是哪個。用 `visualize` 小工具畫出 5 個候選圖示（`FolderPlus`/`Files`/`ClipboardCopy`/`Layers`/`GitFork`）供使用者比較，推薦 `FolderPlus`（跟群組本身的 `Folder` 圖示同一家族，語意最直接），使用者選定。

- **執行內容（單檔小改動，主 session 直接處理、未委派）**：`GroupsPanel.tsx` 的「複製群組」觸發按鈕圖示由 `Copy` 換成 `FolderPlus`；`DuplicateGroupDialog` 內部彈窗提交按鈕的 `Copy` 圖示維持不變（跟「複製為套餐」的觸發按鈕不衝突，且是彈窗內部動作不影響辨識）。

- **修改的既有檔案**：`src/components/GroupsPanel.tsx`
- **build 結果**：✓ 通過，獨立 `eslint --no-eslintrc` 確認無誤；main checkout 合併後重新驗證 build 一次同樣通過
- **commit/push**：`4e9c9e1`（feature 分支），merge 後 main `bb17b78`
- **不滿意的點**：無
- **建議下一步**：無，純視覺辨識度修正
- **已更新常駐檔**：00_執行紀錄.md

---

## [2026-08-11] equipment-cards | 我的關注新增清單/照片顯示切換 | ✓ 完成

- **背景**：使用者希望「我的關注」頁面也能像「設備套餐」頁面一樣，把照片牆切換成純文字品項清單方便快速掃視。畫了排版草稿（頂端全域切換按鈕＋展開後文字列常駐操作圖示）跟使用者確認設計方向（全域切換而非每個群組各自切換；操作圖示直接常駐顯示不用 hover），使用者選「OK，直接執行」。

- **CodeGraph/Grep 調查**：Grep 確認全專案只有 `PhotoWall.tsx` 引用 `<GroupsPanel>`，且只是傳現有 props，本次改動不需新增 prop，確認不影響核心保護元件。

- **執行內容（`frontend` 委派）**：
  1. `useLocalStorageState` hook 複製一份到 `GroupsPanel.tsx`（跟 `PackageExplorer.tsx` 原版邏輯一致，各自維護不共用檔案，避免跨檔案耦合）；`display` state（`'list' | 'photo'`，預設 `'photo'`，storage key `groups_display`，跟套餐頁面的 `packages_own_display`/`packages_shared_display` 不衝突）
  2. 頂端工具列新增清單/照片全域切換按鈕（`ListIcon`/`LayoutGrid`），套用到全部群組
  3. 清單模式下每個群組展開內容改成文字列（料號＋品名），右側常駐操作圖示：非預設群組是替換（`ArrowLeftRight`）/移除（`Minus`），預設群組（我的關注星星群組）是書籤（`Star`）/刪除料卡（`Trash2`），皆沿用既有 handler（`setReplaceTarget`/`handleRemoveCard`/`onToggleBookmark`/`onDelete`），不是另一套邏輯

- **`tester` 驗證**：程式碼追蹤確認 `useLocalStorageState` SSR 安全（無 hydration mismatch）、storage key 不撞名、list/photo 兩模式共用同一份 `displayCards` 渲染來源（不會顯示不一致）、圖示對應群組類型正確、handler 跟 photo 模式共用同一批函式、optional prop 有存在性檢查、篩選正確套用；build 與獨立 `eslint --no-eslintrc` 皆通過。

- **`reviewer` 審查**：正確性/一致性/rename 模式與拖曳排序皆無衝突、無安全性問題，通過。抓到 2 個非阻塞建議，主 session 直接修正：① 移除圖示原用 `X`，跟 `EquipmentCardItem.tsx` 照片模式的 `Minus` 不一致，改用 `Minus` 統一；② `isBookmarked` 在非預設群組分支的 `: false` 是死程式碼（該分支從不讀取這個值），簡化為單純的 `bookmarkedIds?.has(...)`。第 3 項（list 模式列間距跟既有 `AddCardDialog` 略有差異）判斷為刻意的緊湊排版，不修正。

- **技術債（記錄不處理）**：`useLocalStorageState` 沿用套餐頁面既有限制——讀出的 localStorage 字串沒有驗證是否為合法 union 值（非預期值會 fail open 成 list 模式，不會 crash）、無跨分頁 `storage` 事件同步，這兩點是既有模式，非本次新增問題。

- **修改的既有檔案**：`src/components/GroupsPanel.tsx`
- **build 結果**：✓ 通過，獨立 `eslint --no-eslintrc` 確認無誤；main checkout 合併後重新驗證 build 一次同樣通過
- **commit/push**：`c782e2b`（feature 分支），merge 後 main `b1f7a34`
- **不滿意的點**：無
- **建議下一步**：使用者實機驗證清單模式（切換後重新整理頁面偏好是否保留、各群組操作圖示是否正常運作）
- **已更新常駐檔**：00_執行紀錄.md
