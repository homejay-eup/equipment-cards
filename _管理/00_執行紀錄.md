# 00_執行紀錄.md — 試做結果紀錄

> 每次子 Agent 執行完成後追加。只能追加，不修改既有條目。
> 格式：`## [YYYY-MM-DD] {專案ID} | Step {N} | {結果}`

---

## 使用說明

每筆記錄包含：
- **執行內容**：做了什麼
- **產出檔案**：新增或修改的檔案
- **遇到的問題**：執行中的阻礙
- **不滿意的點**：品質未達預期的地方
- **下一步建議**：後續行動

---

## [2026-06-08] P01 | Step 23 | 完成（含 SQL 執行）

**執行內容**：角色群組擴充，前後端程式碼 + SQL 全部完成，build 通過，Vercel 已部署

**產出檔案**：
- `_開發檔案/sql/step23-role-group-expansion.sql`（新建，已在 Supabase 執行完畢）
- `src/app/api/roles/assignable/route.ts`（新建）
- `src/app/api/roles/[id]/permissions/route.ts`（修改：白名單更新）
- `src/app/api/admin/users/route.ts`（修改：指派限制 + null guard 安全修正）
- `src/components/RolesManager.tsx`（修改：UI 按頁面分組、父子連動、移除群組功能、加可編輯議題）
- `src/components/CardFormDialog.tsx`（修改：edit_field_* → edit_card_*）
- `src/app/admin/roles/page.tsx`（修改：加 dept_group/level）
- `src/app/admin/users/page.tsx`（修改：改呼叫 assignable API）

**遇到的問題**：
- SQL 中 `tracker_issues` 表名錯誤（實際為 `issues`），導致 ERROR 42P01，已修正
- `issues.created_by` 是 TEXT，`profiles.id` 是 UUID，需加 `::text` 型別轉換
- SQL tracker key 名稱與現有程式碼不一致（tracker_my_tasks vs view_my_tasks），已修正
- getCallerRoleInfo() null 時安全漏洞，reviewer 發現後修正
- edit_field_* → edit_card_* 更名首次未執行完整，tester 發現後補修
- 前端程式碼跨 main repo 和 worktree 分散，最終同步至 main repo 後一次 commit

**下一步**：Step 23 完成，可依需求討論 Step 24 或其他後續功能

---

## [2026-06-07] P01 | Step 23 | 規格完成（需求討論）

**執行內容**：需求討論並收斂角色群組擴充設計，產出 Step 23 規格文件

**討論決策**：
- 角色內建群組（12 個系統角色），指派角色即決定群組，不另外設定
- 三種資料範圍固定：私有（個人）/ 群組（部門公佈欄）/ 全域
- 追蹤板永遠只看自己 dept_group，包含管理員
- 權限 UI 按頁面分組（料卡列表 / 料卡細節 / 料卡管理 / 帳號管理 / 追蹤板）
- 新增 12 個 `edit_card_*` 細粒度欄位編輯 permission key
- 移除「群組功能」勾選項，追蹤板加「可編輯議題」
- `crud_cards` 拆分為 `create_delete_cards` + `edit_card_*`
- dept_admin 指派限制：只能指派同 dept_group 且 level = member/viewer

**產出檔案**：
- `_管理/01_equipment-cards/specs/step23-role-group-expansion.md`（新增）

**下一步建議**：開新 Session 執行 Step 23，依規格委派 data + frontend

---

## [2026-05-23] P01 | 架構遷移 | 完成

**執行內容**：將舊版平鋪式 CLAUDE.md 結構遷移至 D 型架構（加入 _管理/、.claude/agents/、CoreBrain 連接）

**產出檔案**：
- `.claude/agents/frontend.md`（新增）
- `.claude/agents/data.md`（新增）
- `.claude/agents/tester.md`（新增）
- `.claude/agents/reviewer.md`（新增）
- `_管理/00_專案索引.md`（新增）
- `_管理/00_方案紀錄.md`（新增，含技術選型與 8 項需求決策）
- `_管理/00_執行紀錄.md`（新增，本檔案）
- `_管理/00_待整理清單.md`（新增）
- `_管理/01_equipment-cards/00_專案概覽.md`（新增）
- `_管理/01_equipment-cards/specs/`（新增 8 個規格文件）
- `CLAUDE.md`（改寫為 D 型格式）
- CoreBrain entity/analysis wiki（更新）

**遇到的問題**：無

**不滿意的點**：無

**下一步建議**：確認架構後，開始執行 Step 9（分類標籤更新）

---

## [2026-05-23] P01 | Step 12–15 | 完成

**執行內容**：
- Step 12 — PDF 文件外部連結：`documents JSONB` 欄位（SQL：需手動執行）、API 讀寫、CardDetailDialog 文件顯示、CardFormDialog 文件管理 UI
- Step 13 — 個人備忘板：`user_bookmarks` 資料表 + RLS（SQL：需手動執行）、/api/bookmarks CRUD、PhotoWall「我的關注」tab、CardDetailDialog ⭐ 按鈕 + 個人備註
- Step 14 — 鏡頭功能標籤：CardFormDialog tags datalist 建議、批次打標腳本（未執行）
- Step 15 — 搜尋擴充：Fuse.js keys 新增 category + documents.name

**產出檔案**：
- `src/types/equipment.ts`（新增 Document interface + documents 欄位）
- `src/app/api/cards/route.ts`（加入 documents）
- `src/app/api/cards/[id]/route.ts`（加入 documents）
- `src/app/api/bookmarks/route.ts`（新增）
- `src/app/api/bookmarks/[id]/route.ts`（新增）
- `src/components/CardDetailDialog.tsx`（文件區塊 + ⭐ + 個人備註）
- `src/components/CardFormDialog.tsx`（文件管理 UI + datalist）
- `src/components/PhotoWall.tsx`（我的關注 tab + Fuse keys）
- `_開發檔案/sql/add-documents.sql`（新增）
- `_開發檔案/sql/add-user-bookmarks.sql`（新增）
- `_開發檔案/scripts/tag-lens-cameras.js`（新增，未執行）

**遇到的問題**：
- Supabase DDL 無法自動執行（Management API 需 Personal Access Token），兩個 SQL 需手動到 Dashboard 執行
- Reviewer 發現 documents URL 需過濾 `javascript:` scheme（已修正）

**不滿意的點**：
- pending caption（待上傳細節照片的說明）是 Step 12 前就存在的舊 bug，此次未修正（範圍外）

**下一步建議**：
1. 手動執行兩段 SQL（見 `_開發檔案/sql/`）
2. 驗證 documents 和 bookmarks 功能是否正常
3. 執行 `tag-lens-cameras.js` 前先確認鏡頭料卡數量
4. Step 16（ESG 淨重標記）待排

---

## [2026-05-23] P01 | Step 9 修正 | 完成

**執行內容**：
- 移除 Step 14 加入的 datalist 下拉建議（tags 改回純文字輸入）
- 建立批次更新腳本 `update-categories-and-tags.js`：掃描 `_開發檔案/data/photos-categories` 資料夾，依檔名提取料號 → 建立標籤 map；依料號前綴自動指派分類（1→主機 / 2→鏡頭 / 3→螢幕 / 4→儲存設備 / 5-6→配件 / 7→線材或工具 / 9→耗材或工具，其他→其他）；同步更新 `app_settings` 分類清單
- 執行腳本：487 筆料卡分類更新、488 個料號取得標籤對應（509 筆）、0 失敗
- Build 通過，推送 main 觸發 Vercel 自動部署

**產出檔案**：
- `_開發檔案/scripts/update-categories-and-tags.js`（新增）
- `src/components/CardFormDialog.tsx`（移除 datalist）

**遇到的問題**：
- 腳本路徑錯誤：worktree 中 `__dirname` 深 5 層，需 `path.join(__dirname, '../../../../../')` 才能到主專案根目錄

**不滿意的點**：無

**下一步建議**：
1. 驗證線上分類、標籤是否顯示正確
2. Step 16（ESG 淨重標記）低優先，依需要再排

---

## [2026-05-23] P01 | Step 16 Phase 1 | 完成（SQL 待手動執行）

**執行內容**：ESG 淨重照片標記 — Schema + API + 前端全面實作

**產出檔案**：
- `_開發檔案/sql/add-net-weight.sql`（新增）
- `_管理/01_equipment-cards/specs/esg-net-weight.md`（新增）
- `src/types/equipment.ts`（修改：加入 `net_weight`, `weight_photo`, `weight_photo_public_id`）
- `src/app/api/cards/route.ts`（修改：POST 加入 `net_weight`）
- `src/app/api/cards/[id]/route.ts`（修改：PATCH 加入 `net_weight` + 淨重照刪除邏輯；DELETE 加入 Cloudinary 清除）
- `src/app/api/upload/route.ts`（修改：PATCH 新增 `type: 'weight'` 分支）
- `src/app/api/cards/batch/route.ts`（修改：upsert 行為，回傳含 `updated` 計數）
- `src/components/CardFormDialog.tsx`（修改：淨重輸入 + 淨重照上傳暫存機制）
- `src/components/CardDetailDialog.tsx`（修改：淨重值顯示 + 淨重照加入輪播）
- `src/components/BatchImportDialog.tsx`（修改：header-based CSV 解析 + 淨重欄支援 + BOM 修正）

**遇到的問題**：
- `net_weight = 0` 的字串 falsy 問題（`'0' ? ...` 走 null），改為 `!== ''` 判斷修正
- PATCH /api/cards/[id] 已在 server 端 destroy Cloudinary，client 端不需重複呼叫 DELETE /api/upload
- Excel UTF-8 CSV 帶 BOM 字元導致 header 比對失敗，加 `text.replace(/^﻿/, '')` 修正

**不滿意的點**：無

**下一步建議**：
1. 手動在 Supabase Dashboard 執行 `_開發檔案/sql/add-net-weight.sql`
2. Step 16 Phase 2：等淨重照片提供後，執行批次照片上傳腳本

---

## [2026-06-07] P01 | Step 21 | 完成

**執行內容**：追蹤板 14 項改善（改善 13、14 不動）

**產出檔案**：
- `src/app/tracker/page.tsx`（修改：改為 SSR 渲染 TrackerClient，移除 TrackerList 依賴）
- `src/app/tracker/TrackerClient.tsx`（修改：移除 ArrowLeft 返回按鈕、兩行式佈局、formatDatetime）
- `src/components/IssueDetailDialog.tsx`（修改：Optimistic Update x2、建立者移入 meta grid、formatDatetime、SettingsPopover 齒輪）
- `src/components/NewIssueDialog.tsx`（修改：SettingsPopover 齒輪）
- `src/components/SettingsPopover.tsx`（修改：加入 issueTypes settingKey）
- `src/app/api/settings/route.ts`（修改：PATCH 白名單加入 issueTypes、issueTags）

**遇到的問題**：
- Cherry-pick 時 remote 已重構 tracker 架構（TrackerList → TrackerClient），需整合衝突
- Reviewer 發現 /api/settings PATCH 白名單缺少 issueTypes（Critical）→ 已修正

**不滿意的點**：無

**下一步建議**：git push → Vercel 部署
